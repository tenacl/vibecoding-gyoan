# 비개발자를 위한 바이브코딩 입문 with 클로드 코드

_AI와 대화하며 실제로 쓸 수 있는 서비스를 만드는 3일 집중 과정 — 이론부터 배포·운영까지_

## 과정 소개

### 비개발자를 위한 바이브코딩 입문

_WITH CLAUDE CODE · 3-DAY INTENSIVE_

AI와 대화하며 **실제로 쓸 수 있는 서비스**를 만드는 3일입니다. 코딩 경험이 없어도 괜찮습니다 — 이론은 몰아서 배우지 않고, 배운 즉시 손으로 만들어 확인합니다.

> ⚠️ 시작하기 전에 — 수업 전날까지 끝내야 할 준비물(계정 가입 등)이 있습니다. [「수강 전 준비 체크리스트」 먼저 확인하기 →](#appendix)

> 🎯 **실습 제출하기** → [패들렛에 결과물 올리기](https://padlet.com/ventusion1015_/with-yq4jxkzpqv3t5w02) — 만든 결과물은 여기에 올려 함께 공유해요.

- **① 이론 → 즉시 실습** — 모든 이론은 40분 이내로 끊고, 바로 써먹는 실습이 따라온다.
- **② 웹페이지에서 끝나지 않는다** — 크롬 익스텐션, 데이터가 저장되는 앱까지 "실제로 쓰는 형태"로 만든다.
- **③ 진짜 저장되는 앱** — 3일차에 데이터베이스를 붙인 노션 클론을 만들어 인터넷에 배포한다.

#### 3일의 여정

> (도식: 3일 로드맵 도식)

- **DAY 1 · 기본기** — 개발환경 · 웹의 구조 · 프롬프트 → 내 프로필 페이지를 인터넷에 공개하고, 게임을 만들며 디버깅을 익힌다
- **DAY 2 · 연결** — API 개념 → 구글 API로 크롬 익스텐션, 공공 데이터로 시각화+AI 서비스, 테스트 결제까지
- **DAY 3 · 저장과 공개** — 데이터베이스 입문 → 진짜 저장되는 노션 클론을 만들고, Vercel로 인터넷에 공개한다

#### 이 교안 사용법

- 왼쪽 목차에서 지금 진행 중인 세션을 클릭하면 해당 페이지로 이동합니다
- 목차 상단의 검색창에 "배포", "API", "결제" 같은 단어를 치면 관련 세션만 걸러집니다
- 각 페이지 하단의 이전/다음 버튼으로 수업 순서대로 넘겨볼 수 있습니다
- 어두운 배경 상자 안의 예시 프롬프트는 그대로 복사해서 Claude에게 붙여넣어도 됩니다
- 오른쪽 메모장은 페이지를 넘겨도 유지되고 자동 저장됩니다 — 수업이 끝나면 ⬇ MD로 다운받기로 내 메모를 파일로 가져가세요

> 💡 목차 맨 위의 **「이론 기초」 3장**은 시간표에 묶인 세션이 아니라 **3일 내내 참조하는 배경 지식**입니다. 수업에서는 해당 실습 직전에 필요한 만큼씩 다루고, 궁금할 때 언제든 이 세 장으로 돌아오시면 됩니다.

## 이론 기초

---

### AI가 답하는 원리 — 확률 기계에서 프롬프트까지

_이론 기초 01_

> AI가 **왜** 그렇게 답하는지 알면, **어떻게** 시켜야 하는지가 저절로 따라옵니다.

#### LLM은 "다음 말"을 확률로 고르는 기계

- 110년 동안 사람들이 붙잡은 질문은 딱 하나였습니다 — "다음에 올 말은 무엇인가?"
- 마르코프(1913, 소설을 손으로 세어 통계를 냄) → 섀넌(1948, 언어는 확률의 연쇄) → 신경망 언어모델(2003) → 트랜스포머(2017) → ChatGPT(2022) → 도구를 쥔 AI, 즉 에이전트(지금)
- 기술은 계속 바뀌었지만 원리는 그대로입니다: 앞의 말을 보고 다음에 올 말을 확률로 예측한다

> (도식: 문맥이 쌓일수록 확률 분포가 좁아지는 과정)

#### 왜 같은 질문에 매번 다른 답이 나오나 — 샘플링

- AI는 확률 1등만 고르지 않습니다. 분포에서 추첨(샘플링)하기 때문에 가끔 2등, 3등도 뽑힙니다
- 그래서 답이 매번 달라지는 건 고장이 아니라 설계입니다 — 이걸 알면 "AI가 이상해졌나?"에서 멈추지 않게 됩니다
- 창의성과 환각은 같은 다이얼의 양 끝입니다. 추첨 폭을 넓히면 창의적이 되고, 너무 넓히면 헛소리가 됩니다

#### 환각(할루시네이션) — 원리상 피할 수 없는 것

- 학습에 없는 걸 물어도 AI는 "그럴듯한 다음 단어"를 계속 만들어냅니다. 없는 함수, 없는 링크, 없는 판례를 자신 있게 지어내는 이유입니다
- 다음 단어를 확률로 예측하는 방식의 필연적 결과라서, 프롬프트를 잘 쓴다고 사라지지 않습니다
- ⚠️ 이 문제의 답은 이 과정 마지막에 나옵니다 — 지어내는 대신 도구로 사실을 확인하게 만드는 것(이론 03의 RAG). 지금은 "이런 문제가 있다"만 기억해두세요

#### 그래서 프롬프트는 "확률을 좁히는 행위"입니다

|  | 막연한 요청 | 구체적인 요청 |
| --- | --- | --- |
| **예시** | "맛있는 거 주세요" | "매운 것 빼고, 둘이서 3만 원어치 주세요" |
| **확률 분포** | 평평함 — 후보가 너무 많다 | 뾰족함 — 후보가 좁혀진다 |
| **결과** | 그럴듯한 **평균**이 나온다 | 내가 원하던 것이 나온다 |

#### PTCF — 프롬프트를 구체적으로 쓰는 틀

| 요소 | 묻는 질문 | 예시 |
| --- | --- | --- |
| **P**ersona | 누구로서? | "당신은 동네 카페의 마케터입니다" |
| **T**ask | 무엇을? | "홍보 문구를 3개 써 주세요" |
| **C**ontext | 어떤 상황에서? | "손님 대부분이 출근길 30대 직장인입니다" |
| **F**ormat | 어떤 모양으로? | "문구당 두 문장, 해시태그 3개씩" |

- 네 칸을 매번 다 채울 필요는 없습니다. 쓰기 전에 네 칸을 한 번 떠올리는 습관이 핵심입니다

#### TCREI — 프롬프트를 계속 고쳐가는 틀

| 요소 | 뜻 | 성격 |
| --- | --- | --- |
| **T**ask | 할 일을 한 문장으로 | 프롬프트에 넣는 **재료** |
| **C**ontext | 대상·배경·조건 | 프롬프트에 넣는 **재료** |
| **R**eferences | 예시·원문·양식을 함께 준다 | 프롬프트에 넣는 **재료** |
| **E**valuate | 나온 결과를 그냥 쓰지 않고 **채점**한다 | 답을 받은 뒤 **사람의 행동** |
| **I**terate | 어긋난 부분만 짚어 **다시 시킨다** | 답을 받은 뒤 **사람의 행동** |

- PTCF가 잘 "쓰는" 법이라면, TCREI는 잘 "고쳐가는" 법입니다. 한 번 쓰고 마는 프롬프트가 아니라 계속 다듬어 쓰는 프롬프트에 필요합니다
- AI를 잘 쓰는 사람과 못 쓰는 사람은 뒤의 두 칸(E·I)에서 갈립니다. 첫 결과가 마음에 안 드는 건 정상이고, 거기서 채점하고 다시 시키느냐가 실력입니다

**EXAMPLE — Evaluate → Iterate**
```
1차 결과 채점: 순서 ○, 색 ○, 대표 메뉴 ✕ (세상 카페의 평균이 나옴)

→ 어긋난 칸만 짚어서 다시:
"대표 메뉴를 흑임자 라떼·수제 스콘·핸드드립으로 바꿔 주세요. 나머지는 그대로."
```

#### 프롬프트를 구조화하는 3가지 형식

형식 없는 긴 글은 AI에게 "다 한 줄"로 보입니다. 구역을 나눠주면 조건이 서로 섞이지 않습니다.

| 형식 | 비유 | 언제 쓰나 | 대표 예 |
| --- | --- | --- | --- |
| **마크다운** | 형광펜 | 사람과 AI가 함께 읽는 문서 — **매일 씀** | CLAUDE.md, 이 교안 |
| **XML 태그** | 벽 | 구역이 섞이면 안 될 때 — **가끔** | `<자료>붙여넣은 글</자료>` |
| **JSON** | 자물쇠 | 기계끼리 주고받는 데이터 — **알아만 두기** | API 응답, 툴 정의서 |

> 💡 긴 자료를 붙여넣을 땐 `<자료> … </자료>`로 감싸세요. 이 한 줄 습관이 "AI가 자료 속 문장을 지시로 오해하는" 사고를 막아줍니다. 태그 이름은 아무거나 지어도 됩니다.

#### 마크다운 최소 문법 5개 — 이거면 충분합니다

| 쓰는 법 | 결과 |
| --- | --- |
| `# 제목` | 제목 |
| `**굵게**` | 강조 |
| `- 항목` | 목록 |
| ````코드```` | 코드 블록 |
| `[이름](주소)` | 링크 |

#### 더 깊이 — 공식 자료

**참고 자료**

- [Attention Is All You Need — 트랜스포머 원논문 (2017)](https://arxiv.org/abs/1706.03762) — arxiv.org
- [프롬프트 엔지니어링 공식 문서 (Anthropic)](https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview) — docs.anthropic.com
- [구글 공식 프롬프트 가이드 (PTCF 출처)](https://blog.google/products-and-platforms/products/workspace/google-gemini-workspace-ai-prompt-tips/) — blog.google
- [Google Prompting Essentials (TCREI 출처)](https://grow.google/prompting-essentials/) — grow.google

---

### 에이전트와 하네스

_이론 기초 02_

> 확률 기계에 **도구·기억·순환**을 붙이면 에이전트가 됩니다. 그 에이전트가 일할 환경을 짜는 것이 하네스 엔지니어링입니다.

#### 챗봇과 에이전트 — 기준은 딱 하나

기능 개수가 아니라 **"다음 단계를 누가 정하는가"**로 가릅니다.

|  | 챗봇 | 에이전트 |
| --- | --- | --- |
| **다음 단계를 정하는 주체** | 사람 | AI |
| **비유** | 말하는 백과사전 | 일을 통째로 맡은 담당자 |
| **"이번 주 매출 요약해줘"** | 방법을 알려주고 끝 | 데이터 찾기 → 계산 → 표 만들기 → 검토를 스스로 반복 |

> 💡 앤트로픽 공식 정의도 같은 기준입니다 — **"에이전트는 LLM이 스스로 자신의 프로세스와 도구 사용을 지시하는 시스템"**이고, 미리 정해진 코드 경로를 따르는 건 워크플로우입니다. 한 문장으로는: **"환경 피드백을 받으며 루프 안에서 도구를 쓰는 LLM."**

#### 에이전트의 구성요소

| 구성요소 | 역할 | 없으면 |
| --- | --- | --- |
| **추론 가능한 모델** | 다음에 뭘 할지 판단하는 두뇌 | 아무 결정도 못 한다 |
| **툴(도구)** | 실제로 세상을 읽고 바꾸는 손 | 판단만 하고 아무것도 안 바뀐다 |
| **메모리** | 지금까지 한 일과 알게 된 것을 담는 곳 | 매번 처음부터 다시 시작한다 |
| **(이 셋을 돌리는 루프)** | 목표를 이룰 때까지 반복하고, 다 되면 멈춘다 | 도구를 한 번 쓰는 챗봇에 그친다 |

#### "에이전틱하다"는 무슨 뜻일까

- 시킨 한 가지만 하고 멈추는 게 아니라, 목표를 이루려고 AI가 스스로 할 일을 정해 여러 단계를 알아서 밟아나갈 때 "에이전틱하다(agentic)"고 말합니다
- 비유 — 심부름을 시켰을 때 "재료가 없으면 사 오고, 그것도 없으면 대체품을 찾고, 다 되면 알려주는" 사람이 에이전틱한 사람입니다. "재료 없던데요?" 하고 그 자리에 멈추면 에이전틱하지 않은 것
- 그런 성질을 가진 시스템이 에이전트입니다 — 명령을 받으면 스스로 계획 → 실행 → 결과 판단을 반복하며 목표까지 자율적으로 움직입니다. 바로 위 표의 "다음 단계를 AI가 정한다"가 이 뜻이고, 아래 GRAM 루프가 그 움직임을 그림으로 보여줍니다

#### GRAM 루프 — 에이전트가 도는 순환

> (도식: GRAM 루프 순환 도식)

- 클로드 코드 화면에 "파일 읽는 중", "명령 실행 중"이 지나가면 지금 이 순환이 돌고 있는 것입니다 — 무서운 로그가 아니라 읽을 수 있는 진행 표시입니다
- GRAM은 외울 표가 아니라 이름표입니다. "지금 Reason 중이구나", "방금 건 Action이네"라고 부를 수 있으면 충분합니다
- 영어 자료를 찾을 때는 — 논문·해외 문서에서는 이 순환을 보통 agent loop라고 부르고, 학술 원전은 ReAct(Reason + Act, Yao et al. 2022)입니다

#### 툴로 쓸 수 있는 것들

- 파일 읽기·쓰기·수정 / 명령 실행(터미널) / 검색(파일 안 찾기, 웹 검색) / 브라우저 조작 / 외부 서비스(노션, 깃허브, DB…)
- 외부 서비스를 붙이는 표준 규격이 MCP(Model Context Protocol)입니다 — AI에게 손·눈을 하나씩 달아주는 것이라고 생각하면 됩니다

#### 메모리 — 단기와 장기

|  | 단기 메모리 | 장기 메모리 |
| --- | --- | --- |
| **실체** | **컨텍스트 윈도우** — 지금 대화에 올라와 있는 것 전부 | **기록** — 파일에 적어둔 것 |
| **수명** | 대화가 끝나면 사라짐 | 다음 대화에서도 읽어서 쓸 수 있음 |
| **예** | 방금 주고받은 대화, 읽은 파일 내용 | CLAUDE.md, 작업 노트, 회고 기록 |

#### 컨텍스트 — 한국어로 "맥락"

- 컨텍스트는 우리말로 맥락입니다. AI가 지금 우리 대화를 이해하려고 참고하는 모든 것 — 방금 던진 질문, 앞서 주고받은 대화, 읽은 파일, 붙여넣어 준 자료가 전부 컨텍스트입니다
- AI에게는 눈치도 배경지식도 없어서 컨텍스트에 올라온 것만 압니다. 말하지 않은 사정은 모른다고 생각하는 편이 정확합니다 — 그래서 "무엇을 컨텍스트에 넣어주느냐"가 결과를 좌우합니다
- 이 컨텍스트를 담는 그릇의 크기가 컨텍스트 윈도우, 즉 AI가 한 번에 기억할 수 있는 양입니다 (바로 아래에서 이어집니다)

> 💡 [캡처: 클로드 공식 컨텍스트 윈도우 설명 이미지]

#### 컨텍스트 윈도우 — 왜 새 주제는 새 채팅인가

- AI가 한 번에 기억할 수 있는 양입니다. 무한하지 않습니다
- 중요한 건 용량만이 아닙니다 — 꽉 차기 전부터 품질이 떨어집니다. 시행착오와 버린 코드가 쌓이면 확률 분포가 다시 평평해져서 답이 평균으로 흐릅니다
- 그래서 주제가 바뀌면 새 채팅입니다. 넘쳐서가 아니라 흐려지기 전에 끊는 것입니다

#### 토큰 — 세 갈래로 나눠 보기

| 구분 | 무엇인가 | 알아둘 점 |
| --- | --- | --- |
| **입력 토큰** | 내가 준 것 — 프롬프트, 파일, 이전 대화 | 가장 싸다. 툴 실행 결과도 다음 턴엔 입력으로 쌓인다 |
| **추론 토큰** | AI가 답하기 전에 속으로 생각한 것 | **출력으로 과금됩니다** — 깊이 생각시키는 게 가장 비싸다 |
| **출력 토큰** | 화면에 보이는 답변 | 입력의 **약 5배** 비싸다 |

> 💡 감을 잡는 숫자 하나 — 같은 내용을 반복해서 주면 **캐싱**되어 입력 비용이 1/10로 떨어집니다. 그러면 **출력 토큰 1개 ≈ 캐시된 입력 토큰 50개**가 됩니다. "자료를 많이 읽히는 것"보다 **"길게 생각하고 길게 답하게 하는 것"이 훨씬 비쌉니다.**

#### 컨텍스트 엔지니어링

- 그래서 질문이 생깁니다 — "이 작업에 어떤 컨텍스트를 줘야 하는가?"
- 필요한 건 넣고, 필요 없는 건 빼고, 순서를 정하는 것. 이 고민 자체를 컨텍스트 엔지니어링이라고 부릅니다
- 많이 넣는 게 좋은 게 아닙니다 — 관계없는 자료는 분포를 흐리고 비용만 늘립니다

#### 에이전트 문서 — 규칙을 어디에 적어두나

에이전트가 지켜야 할 규칙과 기준을 적어두는 문서들입니다. **로드되는 시점이 서로 다르다**는 게 핵심입니다.

> (도식: 에이전트 문서의 3단 로딩 구조)

| 문서 | 정체 | 로드 시점 |
| --- | --- | --- |
| **CLAUDE.md**<br>(범용 표준은 **AGENTS.md**) | 프로젝트 규칙·기준 문서 | 새 작업을 시작할 때 **항상** |
| **Rules** | 주제별로 나눈 규칙 파일 | 해당 경로의 파일을 다룰 때만 |
| **Skills** (폴더 안의 `SKILL.md`) | 작업 절차를 적어둔 재사용 지시서 | 필요할 때 **찾아서** 로드 |

- 지침서가 길면 실제 작업에 쓸 컨텍스트가 줄어듭니다. 그래서 길게 쓰지 말고 개념 단위로 쪼개서, 필요할 때 찾아 쓰게 만드는 게 좋습니다 — Skills가 정확히 그 문제를 풀려고 만들어진 기능입니다
- ⚠️ Hooks는 문서가 아닙니다. 정해진 시점에 반드시 실행되는 자동 명령(코드)이고, 컨텍스트를 차지하지도 않습니다. 문서 3종이 부탁이라면 훅은 강제입니다

#### 하네스 엔지니어링

- 하네스는 원래 말을 부릴 때 쓰는 장비입니다. 에이전트가 일할 프로젝트 환경 전체를 설계하는 것이 하네스 엔지니어링입니다
- 무엇을 짜나 — 규칙 문서(CLAUDE.md·Rules·Skills), 권한 범위, 쓸 수 있는 도구, 강제 장치(훅), 그리고 결과를 확인하는 방법
- 같은 AI라도 하네스가 잘 짜인 프로젝트에서 훨씬 잘 일합니다. 모델을 바꾸는 것보다 환경을 고치는 편이 빠를 때가 많습니다

#### 더 깊이 — 공식 자료

**참고 자료**

- [Building Effective Agents — 에이전트 정의 (Anthropic 공식, 영문)](https://www.anthropic.com/engineering/building-effective-agents) — anthropic.com
- [ReAct — GRAM 루프의 학술 원전 (Yao et al. 2022)](https://arxiv.org/abs/2210.03629) — arxiv.org
- [CLAUDE.md(메모리) 공식 문서](https://code.claude.com/docs/en/memory) — code.claude.com
- [Skill 공식 문서](https://code.claude.com/docs/en/skills) — code.claude.com
- [Hooks 공식 가이드](https://code.claude.com/docs/en/hooks-guide) — code.claude.com

---

### RAG와 에이전틱 서치 — 환각에 대한 답

_이론 기초 03_

> 이론 01에서 던진 문제, 기억하시나요? **AI는 모르는 것도 그럴듯하게 지어냅니다.** 그 문제의 답이 여기 있습니다.

#### 답은 "지어내지 말고, 찾아보게 하는 것"

- 확률로 다음 단어를 만들어내는 대신, 실제 자료를 읽고 그걸 근거로 답하게 만듭니다
- 이게 도구(툴)가 존재하는 근본적인 이유입니다 — 검색하고, 파일을 읽고, 코드를 실행해서 사실을 확인합니다
- 앤트로픽이 에이전트를 "환경 피드백을 받으며 루프를 도는 LLM"이라고 정의하는 이유이기도 합니다. 여기서 환경 피드백이 곧 사실 확인입니다

#### RAG — 검색해서 근거를 붙인 뒤 답하기

- RAG = Retrieval-Augmented Generation. 우리말로 하면 "검색으로 보강한 생성"입니다
- 흐름은 단순합니다: 질문이 오면 → 관련 자료를 먼저 찾고 → 그 자료를 함께 넣어서 → 답하게 한다
- 흔한 오해 하나 — RAG는 벡터 데이터베이스를 뜻하는 게 아닙니다. 찾아오는 방법은 무엇이든 됩니다. 클로드 코드가 내 폴더에서 파일을 검색해 읽고 답하는 것도 넓은 의미의 RAG입니다

> **⚠️ 가장 중요한 주의 — RAG는 환각을 없애지 않습니다.**
> 찾아온 자료 자체가 틀렸을 수도, 맞는 자료를 잘못 읽을 수도, 자료에 없는 내용을 유추해 덧붙일 수도 있습니다. RAG가 하는 일은 환각을 **제거**하는 게 아니라 **근거를 붙여 검증 가능하게** 만드는 것입니다. "RAG를 쓰면 환각이 사라진다"는 흔한 과장이고, 오히려 **결과를 확인하는 습관이 더 중요해집니다.**

#### 에이전틱 서치 — 검색을 에이전트가 스스로

> (도식: RAG와 에이전틱 서치 비교)

- RAG는 정해진 방식으로 한 번 찾아서 넣어줍니다. 에이전틱 서치는 에이전트가 무엇을 찾을지 스스로 판단하고, 결과를 보고 부족하면 다시 찾습니다
- 둘은 대체 관계가 아니라 겹쳐 쓰는 관계입니다 — 에이전틱 서치는 RAG에 GRAM 루프를 얹은 것이라고 보면 됩니다
- 기준이 낯익지 않으신가요? "다음 단계를 누가 정하는가" — 이론 02에서 챗봇과 에이전트를 가른 그 기준입니다

#### LLM 위키 — 찾아 쓸 지식을 아예 쌓아두기

- RAG가 "필요할 때 찾아온다"면, 그 찾아올 대상을 미리 잘 만들어두자는 흐름이 있습니다. AI가 읽기 좋게 정리해둔 지식 창고를 LLM 위키라고 부릅니다
- 거창한 게 아닙니다 — 마크다운 파일을 폴더로 정리한 것입니다. 표 설명, 용어 정의, 업무 절차, 과거 사고 기록 같은 것을 문서로 쌓아두고 에이전트가 참조하게 합니다
- 이 패턴은 이미 여러 이름으로 존재해 왔습니다 — 우리가 배운 CLAUDE.md도 그중 하나입니다. 구글 클라우드가 이걸 공통 규격으로 정리한 것이 OKF(Open Knowledge Format)이고, 특별한 도구 없이 마크다운 + 폴더 구조가 전부입니다
- 왜 중요한가 — 회사에서 진짜 필요한 지식은 대부분 내부 지식(우리 회사의 지표 정의, 이 표가 뭘 뜻하는지, 장애 대응 절차)이고, 이건 AI가 학습한 적이 없습니다. 위키에 쌓아두면 매번 설명하지 않아도 됩니다

> 💡 이론 02의 **장기 메모리** 기억하시나요? LLM 위키는 그 장기 메모리를 **개인에서 팀·조직 단위로 넓힌 것**입니다. 오늘 CLAUDE.md 한 장을 쓰는 습관이, 회사에서는 팀 지식 창고를 만드는 일로 이어집니다.

#### 컨텍스트 엔지니어링과의 관계

- 이론 02에서 컨텍스트 엔지니어링을 "어떤 컨텍스트를 줄지 고민하는 것"이라고 했습니다
- RAG는 그 고민을 자동화한 것입니다 — 사람이 매번 골라 넣던 자료를, 시스템이 질문에 맞춰 대신 찾아 넣어줍니다
- 컨텍스트 윈도우에 모든 걸 넣을 수 없기 때문에 필요한 기술이기도 합니다. 다 넣으면 흐려지고, 안 넣으면 지어냅니다 — 그 사이를 푸는 게 RAG입니다

> 💡 3일 과정 전체를 한 줄로 이으면 이렇게 됩니다 — **확률 기계(이론 01) → 잘 시키는 법(프롬프트) → 도구와 순환을 붙여 에이전트로(이론 02) → 사실을 확인하게 만들기(이론 03)**. 실습에서 만드는 모든 것이 이 사슬 위에 있습니다.

#### 더 깊이 — 공식 자료

**참고 자료**

- [Building Effective Agents (Anthropic 공식, 영문)](https://www.anthropic.com/engineering/building-effective-agents) — anthropic.com
- [RAG 원논문 — Retrieval-Augmented Generation (Lewis et al. 2020)](https://arxiv.org/abs/2005.11401) — arxiv.org
- [Open Knowledge Format — LLM 위키를 규격화한 문서 (구글 클라우드)](https://cloud.google.com/blog/products/data-analytics/how-the-open-knowledge-format-can-improve-data-sharing) — cloud.google.com

## DAY 1 — 기본기

---

### 기본기 — 만들고, 고치고, 배포한다

_DAY 1_

> 오늘의 목표: 아이디어 → 배포된 웹페이지까지 전체 사이클을 두 번 경험한다.

> 오늘의 산출물 — **인터넷에 배포된 내 프로필 페이지 URL 1개 + 플레이 가능한 게임 1개**

오전에는 도구(개발환경), 재료(웹의 구조), 기술(프롬프트)을 익히고, 오후에는 그걸로 실제 프로젝트 두 개를 완성합니다. 퇴근 전에 여러분의 웹페이지 링크를 가족·친구에게 보낼 수 있게 됩니다.

#### 시간표

| 시간 | 구분 | 세션 |
| --- | --- | --- |
| 09:00–09:30 | 이론 | [오리엔테이션 · 바이브코딩이란](#d1-1) |
| 09:30–10:20 | 이론+실습 | [개발환경 구성 + 첫 페이지 만들기](#d1-2) |
| 10:30–11:10 | 이론+실습 | [웹의 구조 (프론트엔드/백엔드/DB)](#d1-3) |
| 11:10–12:00 | 이론+실습 | [프롬프트 엔지니어링 + 토큰 절약](#d1-4) |
| 13:00–13:30 | 이론 | [바이브코딩 플로우 + 깃](#d1-5) |
| 13:30–15:00 | 프로젝트 | [프로젝트 1 · 나만의 프로필 페이지](#d1-6) |
| 15:10–16:40 | 프로젝트 | [프로젝트 2 · 고전게임 만들기](#d1-7) |
| 16:40–17:00 | 회고 | [Day 1 회고](#d1-8) |

---

### 오리엔테이션 · 바이브코딩이란

_DAY 1 · 09:00–09:30 · 이론_

> 코드를 직접 타이핑하지 않아도 됩니다. AI에게 한국어로 설명해서 만드는 개발 — 그게 바이브코딩입니다.

> (도식: 바이브코딩의 순환 구조 도식)

#### 핵심 내용

- 바이브코딩 = AI에게 "이런 걸 만들어줘"라고 말로 설명하면 AI가 코드를 대신 써주는 개발 방식. 우리는 코드를 "쓰는 사람"이 아니라 "시키고, 확인하는 사람"이 된다
- 그래서 필요한 실력도 다르다 — 문법 암기 ❌ / 원하는 것을 정확히 설명하는 능력과 결과가 맞는지 판단하는 능력 ⭕
- 코드를 몰라도 시작할 수 있지만, 웹이 어떤 부품으로 이루어지는지 큰 그림은 알아야 잘 시킬 수 있다 — 그래서 오늘 오전에 그 큰 그림부터 배운다
- 3일 로드맵: Day 1 만들고 인터넷에 공개하기 → Day 2 바깥 데이터와 연결하기(API) → Day 3 데이터베이스로 진짜 저장되는 앱(노션 클론) 만들어 인터넷에 공개하기

#### "바이브코딩"이라는 말은 어디서 왔나

> 복잡한 코드를 직접 써 내려가는 일이 아닙니다. 코딩 에이전트와 대화하며, 만들고 싶은 결과물을 정확히 설명하고 이해시키는 일입니다.

- 바이브코딩은 OpenAI 창립 멤버 안드레이 카파시(Andrej Karpathy)가 2025년 초 처음 꺼낸 말이다 — "코드를 한 줄씩 붙잡고 있는 게 아니라, 흐름(vibe)에 몸을 맡기고 AI에게 시키며 만든다"는 뜻에서 나왔다
- 그래서 핵심은 타이핑 실력이 아니라 "무엇을, 왜 만들고 싶은지"를 말로 또렷하게 전달하는 힘이다 — 요리로 치면 내가 칼질을 하는 게 아니라, 주방장(AI)에게 원하는 요리를 정확히 주문하고 맛을 보는 사람이 되는 것
- 주문이 뭉뚱그려지면 엉뚱한 게 나오고, 구체적으로 설명할수록 원하는 결과에 가까워진다 — 이 "설명하고 → 확인하고 → 다시 요청하는" 감각이 3일 내내 여러분이 기를 진짜 실력이다

#### 일반 챗봇과 뭐가 다른가

|  | 일반 챗봇 | 클로드 코드 |
| --- | --- | --- |
| **역할** | 조언하는 컨설턴트 | 직접 일하는 개발자 |
| **코드** | 화면에 보여주기만 — 내가 복사해서 붙여야 함 | 파일을 스스로 만들고 고침 |
| **실행** | 명령 실행 불가 | 터미널에서 직접 실행하고 확인 |
| **에러 대응** | 해결법을 설명해줌 | 스크린샷 붙여넣으면 즉시 분석하고 직접 고침 |
| **체감** | 매번 복붙 왕복 | "가계부 만들어줘" 한 마디면 보통 2~5분에 완성 |

#### 미리 알아두면 마음 편한 것 2가지

- AI는 확률 기계다 — 같은 질문에 매번 조금씩 다른 답이 나오는 건 고장이 아니라 원래 그렇게 설계된 것. 그리고 맥락(자료·설명)을 많이 줄수록 답이 정확해진다 — 오전에 배울 프롬프트 기술의 원리다
- 정말 어려운 건 개발이 아니라 가입·설치·연결이다 — 계정 만들기, 키 발급에서 막히는 게 정상이다. 막히면 여러분 잘못이 아니니 바로 손 들자 (부록 「막혔을 때 응급처치」 참고)

#### 이 교육에서 쓸 "바이브코딩 6단계" — 3일 내내 반복할 흐름

> 기억할 건 딱 하나입니다. 아래 6단계를 한 바퀴 돌리면 결과물 하나가 세상에 나옵니다. 그리고 이 바퀴를 3일 동안 계속 돌립니다.

> (도식: 바이브코딩 6단계 흐름 도식: 폴더 생성 → Plan 계획 → Build 구현 → 검토·개선 → 배포 → 공유, 그리고 반복)

> 💡 이 6단계는 한 번 쓰고 버리는 순서가 아닙니다. **작은 결과물 하나를 만들 때마다 ①~⑥을 반복**합니다 — 3일 동안 이 바퀴를 여러 번 돌리다 보면, 어느새 몸에 배어 여러분만의 서비스도 같은 방식으로 만들게 됩니다.

> 💡 오늘의 약속 — 퇴근 전에 여러분 각자의 웹페이지가 인터넷에 올라가고, 누구에게든 링크를 보낼 수 있게 됩니다.

#### 더 깊이 — 공식 자료

**참고 자료**

- [Claude 요금제 안내](https://claude.com/pricing) — claude.com
- [AI 에이전트란 무엇인가 (Anthropic 공식 글, 영문)](https://www.anthropic.com/engineering/building-effective-agents) — anthropic.com

---

### 개발환경 구성 + 첫 페이지 만들기

_DAY 1 · 09:30–10:20 · 이론 · 실습_

> Claude를 쓸 수 있는 3가지 방법을 알아보고, 수업용 환경을 세팅해 첫 결과물을 띄웁니다.

#### Claude를 쓰는 3가지 창구

| 환경 | 모습 | 어울리는 일 |
| --- | --- | --- |
| **데스크톱 앱** | 메신저처럼 대화하는 창 | 질문, 아이디어 정리, 문서 작업 |
| **VS Code 확장** | 코드 편집기 안에 Claude가 들어온 형태 | 파일을 눈으로 보면서 시키기 |
| **Claude Code (터미널)** | 명령어 창에서 도는 에이전트 | 파일 생성·수정을 알아서 처리 — 가장 강력 |

- 수업에서는 한 가지 환경으로 전원 통일해서 진행한다 — 지금부터 다 같이 설치한다

#### 따라하기 ① — 윈도우에 Claude Code 설치

**설치 순서**

1. 시작 전 확인 — Claude 유료 구독(Pro 이상) 계정으로 로그인할 수 있어야 한다. 아직 무료 계정이라면 지금 업그레이드부터 (뒤에서 로그인이 막히면 대부분 이 이유다)
2. PowerShell 열기 — 키보드에서 윈도우 키를 누르고 "powershell"이라고 친 뒤 Enter (관리자 권한 필요 없음)
3. 아래 명령어를 복사해서 PowerShell 창에 붙여넣고(마우스 우클릭 = 붙여넣기) Enter — 설치가 자동으로 진행된다
4. 설치가 끝나면 PowerShell 창을 닫았다가 새로 연다 (새 창이어야 설치를 인식한다)
5. 새 창에서 claude --version을 쳐본다 — 버전 번호가 나오면 설치 성공
6. claude를 치면 첫 실행 — 브라우저가 열리면서 로그인 화면이 뜬다. Claude 계정으로 로그인하고, 화면에 코드가 나오면 복사해서 터미널에 붙여넣는다. "Login successful"이 뜨면 끝. 참고로 클로드 코드는 유료 구독(Pro 이상) 계정이어야 쓸 수 있다 — 로그인이 됐는데도 안 된다면 대부분 이 이유다

**POWERSHELL — 설치 명령어 (한 줄 그대로 복사)**
```
irm https://claude.ai/install.ps1 | iex
```

- 참고 — Node.js가 이미 설치된 컴퓨터라면 npm install -g @anthropic-ai/claude-code로도 설치할 수 있다 (Node.js 22 이상). 오늘은 위의 한 줄 방식으로 통일한다
- VS Code 확장은 VS Code 왼쪽의 확장(Extensions) 아이콘 → "Claude Code" 검색 → 설치

#### 환경변수 — 컴퓨터 전체가 기억하는 설정

- 환경변수 = 특정 프로그램이 아니라 내 컴퓨터 전체에 붙여두는 메모. 어떤 프로그램이든 이 메모를 읽을 수 있다
- 대표적인 환경변수가 Path — "명령어를 치면 어느 폴더에서 프로그램을 찾을지"의 목록. 터미널에서 claude만 쳐도 실행되는 건 Path에 설치 폴더가 등록되어 있기 때문이다
- 그래서 `claude` 명령을 찾을 수 없다는 에러가 나면 = Path에 설치 폴더가 등록 안 된 것. 아래 방법으로 직접 등록하면 해결된다
- 미리 알아두면 좋은 이유 — 내일(Day 2) 배우는 API 키도 환경변수로 보관한다. 코드에 키를 직접 적지 않는 안전한 방법이 바로 이것이다

**따라하기 ② — 환경변수 창 열고 Path 등록하기**

1. 먼저 확인 — 위에서 claude --version이 잘 나왔다면 이 절차는 건너뛰어도 된다. "명령을 찾을 수 없다"는 에러가 났을 때만 따라 하자
2. 윈도우 키를 누르고 "환경 변수"라고 검색 → "시스템 환경 변수 편집" 클릭 → 창이 뜨면 아래쪽 [환경 변수] 버튼 클릭
3. 위쪽 "사용자 변수" 목록에서 Path를 선택하고 [편집] 클릭
4. [새로 만들기] 클릭 → 아래 경로를 입력하고 확인을 눌러 모든 창을 닫는다
5. 열려 있던 PowerShell을 닫았다가 새로 열고 claude --version으로 확인 — 환경변수는 새 창부터 적용된다

**PATH에 추가할 경로 (Claude Code가 설치되는 위치)**
```
%USERPROFILE%\.local\bin
```

> 💡 새 환경변수를 만들 때도 같은 창을 씁니다 — "사용자 변수"에서 **[새로 만들기]** → 변수 이름과 값 입력. 내일 API 키를 보관할 때 이 방법을 다시 만나게 됩니다.

#### 터미널이란 — 마우스 대신 '말로' 시키는 창

- 터미널(=PowerShell 같은 명령어 창) = 평소 마우스로 하던 일을 글자 명령어로 대신 시키는 프로그램. 폴더를 더블클릭해서 들어가는 대신 cd 폴더명이라고 쳐서 들어간다
- 검은 화면에 글씨만 있어서 낯설 뿐, 어렵지 않다 — 우리가 외울 명령어는 대여섯 개면 충분하다. 나머지는 전부 Claude가 알아서 친다
- 왜 배우나 — 가장 강력한 Claude Code가 바로 이 터미널 위에서 돌기 때문이다. 터미널을 조금 알면 클로드 코드가 무슨 일을 하는지 눈에 들어온다

> 💡 [캡처/짤: 영화 매트릭스에서 트리니티가 터미널로 해킹하는 장면] — 영화 속 해커들이 검은 화면에 글자를 치는 그 창이 바로 터미널입니다. 생각보다 우리 일상과 가깝습니다.

#### 왜 코딩 에이전트에겐 터미널이 잘 맞나

- Claude 같은 LLM은 '언어'를 학습한 모델이다 — 그림보다 글(언어)로 주고받을 때 가장 빠르고 정확하다
- 터미널은 모든 작업이 글자(명령어와 글자 결과)로 오간다. 그래서 마우스로 버튼을 찾아 누르는 화면 조작보다, 언어로 처리하는 터미널이 AI에겐 훨씬 효율적이고 빠른 작업 방식이다
- 정리하면 — 사람에겐 마우스, AI에겐 터미널이 편하다. 그래서 가장 일 잘하는 AI 도구가 터미널에서 도는 것이다

#### 자주 쓰는 터미널 명령어 (이것만)

| 명령어 | 하는 일 | 예시 |
| --- | --- | --- |
| `cd 폴더명` | 그 폴더 **안으로** 들어가기 (change directory) | `cd vibe-day1` |
| `cd ..` | 한 단계 **위 폴더**로 나가기 (`..` = 상위 폴더) | `cd ..` |
| `ls` (윈도우: `dir`) | 지금 폴더 안의 **파일 목록** 보기 | `ls` |
| `pwd` | 지금 내가 **어느 폴더**에 있는지 전체 주소로 보기 | `pwd` |
| `mkdir 이름` | 새 **폴더 만들기** (make directory) | `mkdir images` |
| `cls` / `clear` | 지저분해진 **터미널 화면 청소** | `cls` |

- 외우기 부담 갖지 말자 — 제일 많이 쓰는 건 `cd`(이동) 하나다. 나머지는 "아, 이런 게 있었지" 정도면 된다

#### 절대경로 vs 상대경로 — 길 찾는 두 가지 방법

- 비유로 — 친구에게 우리 집을 알려줄 때 두 가지 방법이 있다. ① "서울시 ○○구 ○○로 12"처럼 처음부터 끝까지 다 말하기(절대경로) ② "여기서 오른쪽 두 번째 골목 왼쪽 집"처럼 지금 위치 기준으로 말하기(상대경로)

| 구분 | 뜻 | 예시 |
| --- | --- | --- |
| **절대경로** | **뿌리(드라이브)부터** 끝까지 적은 전체 주소 — 어디서 봐도 딱 한 곳 | `C:\Users\내이름\Desktop\vibe-day1\index.html` |
| **상대경로** | **지금 내 위치 기준**의 짧은 주소 — 위치가 바뀌면 가리키는 곳도 바뀐다 | `index.html` · `images/logo.png` · `../`(한 칸 위) |

- Claude가 "경로를 못 찾겠다"고 할 때 대부분은 위치 착각이다 — pwd로 지금 위치를 확인하거나, 헷갈리면 절대경로(전체 주소)로 말해주면 확실하다

#### 로컬호스트 · 포트 · 서버 — 어렵지 않습니다

- 서버 = 거창한 게 아니라 그냥 24시간 켜져 있는 컴퓨터다. 우리가 인스타에 접속하면, 저쪽에서 늘 켜져 있는 컴퓨터(서버)가 화면을 내려준다 — 그게 전부다
- 로컬호스트(localhost) = 바로 내 컴퓨터 자신을 가리키는 이름(주소로는 127.0.0.1). 내가 만든 걸 남에게 공개하기 전에 나만 먼저 테스트하는 '내 컴퓨터 안 미리보기'다
- 포트(port) = 컴퓨터라는 건물의 문 번호. 한 컴퓨터가 여러 프로그램을 동시에 돌리니, "이 프로그램은 3000번 문, 저건 5500번 문" 식으로 문을 나눠 쓴다
- 그래서 http://localhost:3000 은 "내 컴퓨터(localhost)의 3000번 문(port)으로 들어와"라는 뜻이다. Claude가 "브라우저에서 localhost:3000 을 열어보세요"라고 하면 겁먹지 말고 그 주소를 주소창에 붙여넣으면 된다

> 💡 정리 — **서버 = 늘 켜진 컴퓨터, 로컬호스트 = 내 컴퓨터, 포트 = 문 번호**. 이 세 단어만 잡아두면 Day 2에서 서버를 다룰 때 훨씬 편합니다.

#### 쓰기 전에 알아둘 것

- 반드시 프로젝트 폴더 안에서 실행한다 — 클로드 코드는 "지금 열려 있는 폴더"를 기준으로 일한다. 바탕화면에서 실행하면 바탕화면 전체가 프로젝트가 되어버린다 (가장 흔한 실수 1위)
- 스크린샷 한 장이 설명 열 줄보다 낫다 — Win+Shift+S로 화면을 캡처하고, 클로드 코드 창에 Ctrl+V로 붙여넣을 수 있다. 에러 화면·참고 디자인 공유에 최고

#### 허락을 물어보면 이렇게 답한다

| 입력 | 뜻 | 언제 쓰나 |
| --- | --- | --- |
| **y** | 이번 한 번 허락 | 처음엔 이걸로 하나씩 확인하며 진행 |
| **n** | 거부 | 의도와 다른 일을 하려고 할 때 |
| **a** | 이 대화에서는 계속 허락 | 익숙해져서 믿고 맡길 때 (속도 ↑) |

#### 기본 명령어

| 명령어 | 뜻 |
| --- | --- |
| `/init` | "이 프로젝트 파악해서 설명서(CLAUDE.md) 만들어줘" |
| `/clear` | "대화 비우고 새로 시작" |
| `/compact` | "대화가 길어졌을 때 요약해서 이어가기" |
| **계획 모드** | "실행 전에 계획부터 보여줘" |

- 용어 하나만 기억하자: 프롬프트 = AI에게 보내는 요청 문장. 3일 내내 우리의 주력 도구다

#### 슬래시(/) 명령어 — 더 알아두면 좋은 것들

- 클로드 코드 입력창에 `/`만 쳐도 쓸 수 있는 명령어 목록이 뜬다. 앞서 본 /init·/clear·/compact 외에 자주 쓰는 것들:

| 명령어 | 하는 일 |
| --- | --- |
| `/help` | "쓸 수 있는 명령어랑 사용법 보여줘" |
| `/model` | "어떤 AI 모델을 쓸지 고르기" (똑똑함 ↔ 속도) |
| `/cost` | "지금까지 이 대화에서 쓴 사용량 보여줘" |
| `/resume` | "이전에 하던 대화를 이어서 다시 열기" |
| **Esc** 키 | AI가 엉뚱한 길로 갈 때 **즉시 멈추기** (명령어 아님, 그냥 Esc) |

#### 클로드 코드의 4가지 실행 모드

- 클로드 코드는 "얼마나 알아서 진행할지"를 모드로 정한다. 입력창에서 Shift+Tab을 누를 때마다 모드가 차례로 바뀐다 — 처음엔 확인하며(편집 모드), 익숙해지면 맡기며(오토 모드) 쓰면 된다

| 모드 | 무엇을 하나 | 언제 쓰나 |
| --- | --- | --- |
| **플랜 모드** | 파일을 **건드리지 않고 계획만** 세워 보여준다 (읽기 전용) | 큰 작업 전에 방향부터 잡고 싶을 때 — 강력 추천 |
| **편집(수락) 모드** | 파일을 바꾸기 전에 **매번 y/n으로 허락**을 묻는다 | 기본값. 처음엔 하나씩 확인하며 진행 (가장 안전) |
| **오토(자동수락) 모드** | 파일 수정을 **일일이 묻지 않고 알아서** 진행한다 | 믿고 맡길 만큼 익숙해졌을 때 (속도 ↑) |
| **바이패스 모드**<br>(`--dangerously-skip-permissions`) | 모든 **권한 확인을 건너뛰고** 무엇이든 그냥 실행 | 원칙적으로 **쓰지 않기** — 아래 경고 참고 |

> ⚠️ **바이패스 모드 주의** — 이름 그대로 `--dangerously`(위험하게)입니다. 파일 삭제 같은 일까지 **물어보지 않고 실행**하므로, 되돌릴 수 없는 사고가 날 수 있습니다. **수업에서는 켜지 마세요.** 편집 모드(y/n 확인) 또는 오토 모드까지만으로 충분합니다.

#### 플랜 모드 + 되묻기(AskUserQuestion) 활용법

- 큰 걸 시킬 땐 바로 만들게 하지 말고, 먼저 플랜 모드로 계획을 세우게 하자. 계획을 눈으로 보고 승인해야 실제 작업이 시작되니 엉뚱한 결과·되돌리기가 확 줄어든다
- 여기에 "애매하면 나에게 되물어봐"를 붙이면, Claude가 혼자 넘겨짚는 대신 몇 가지 선택지를 물어본다(AskUserQuestion). 답만 고르면 방향이 저절로 좁혀진다 — 긴 프롬프트를 안 써도 된다

**EXAMPLE PROMPT (플랜 모드에서 계획 먼저 + 되묻기)**
```
동네 카페 소개 페이지를 만들 거야. 바로 만들지 말고 먼저 계획을 세워서 보여줘.
정하기 애매한 부분(색감, 페이지 구성, 담을 내용)은 나에게 선택지로 되물어봐.
내가 계획에 OK 하면 그때 만들기 시작해줘.
```

#### 클로드 코드는 '지침서'부터 읽고 시작한다

- 클로드 코드는 일을 시작할 때, 프로젝트 폴더에 있는 규칙 문서(대표적으로 `CLAUDE.md`)를 먼저 읽는다. 그 내용을 바탕 맥락으로 깔고 그 위에서 우리 요청을 처리한다
- 덕분에 "한국어로 답해줘" 같은 반복 지시를 매번 안 해도 되고, 대화를 /clear 해도 규칙은 계속 유지된다 — 새 대화마다 지침서를 다시 읽기 때문이다
- 이 지침서를 어떻게 잘 쓰는지는 Day 2 'CLAUDE.md와 Skill' 세션에서 제대로 다룬다. 여기서는 "클로드 코드는 규칙 문서를 먼저 읽고 그 맥락 위에서 움직인다"는 원리만 기억하면 된다

**실습 — 첫 프롬프트**

1. 바탕화면에 vibe-day1 폴더를 만든다 — 이 폴더가 곧 "프로젝트"다. 아까 "바탕화면에서 실행 금지"라고 한 것과 헷갈리지 말자: 바탕화면 자체가 아니라, 그 안에 만든 이 폴더 안에서 실행하면 된다
2. 만든 폴더를 연 다음, 파일 탐색기 위쪽의 주소창을 클릭하고 powershell이라고 친 뒤 Enter — 그러면 그 폴더 위치에서 터미널이 열린다. 여기서 claude를 입력하면 시작된다
3. 아래 프롬프트를 보낸다
4. 폴더에 생긴 index.html 파일을 더블클릭해 브라우저로 연다
5. 화면이 뜨면 성공! 오늘의 첫 작품이다 — 강사 화면과 똑같이 안 나와도 정상입니다. AI는 매번 조금씩 다르게 만듭니다(이론 01의 샘플링)
6. 완성 화면을 Win+Shift+S로 캡처해 폴더에 모아두자 — 3일 뒤 회고와 결과물 공유의 재료가 된다

**EXAMPLE PROMPT**
```
내 이름과 오늘 날짜, "나의 첫 바이브코딩"이라는 제목이 있는
한 페이지짜리 HTML을 만들어줘. 배경은 밝은 색, 글자는 화면 가운데 크게.
```

#### 이 장을 마치면

- [ ] 터미널에 claude --version을 치면 버전 번호가 나온다
- [ ] 내가 만든 index.html이 브라우저에서 열린다
- [ ] "프롬프트가 뭐예요?"라는 질문에 한 문장으로 답할 수 있다

#### 더 깊이 — 공식 자료

**참고 자료**

- [Claude Code 설치 공식 문서](https://code.claude.com/docs/en/setup) — code.claude.com
- [Claude Code 퀵스타트 (첫 사용법)](https://code.claude.com/docs/en/quickstart) — code.claude.com
- [슬래시 명령어 전체 목록](https://code.claude.com/docs/en/slash-commands) — code.claude.com
- [VS Code에서 Claude Code 쓰기](https://code.claude.com/docs/en/vs-code) — code.claude.com

---

### 웹의 구조 — 프론트엔드 / 백엔드 / 데이터베이스

_DAY 1 · 10:30–11:10 · 이론 · 실습_

> 웹 서비스는 딱 3개의 부품으로 이루어집니다. 이 큰 그림만 잡으면 AI에게 훨씬 잘 시킬 수 있습니다.

> (도식: 프론트엔드, 백엔드, 데이터베이스 구조 도식)

#### 3개의 부품 한눈에 보기

| 부품 | 하는 일 | 인스타그램으로 치면 |
| --- | --- | --- |
| **프론트엔드** (앞단) | 사용자 눈에 보이는 전부 — HTML(뼈대) · CSS(모양) · JS(움직임) | 피드 화면 |
| **백엔드** (뒷단) | 화면 뒤에서 일하는 서버 — 로그인 확인, 주문 접수처럼 화면에 못 맡기는 일 | 좋아요를 접수하는 서버 |
| **데이터베이스** (DB) | 껐다 켜도 남아야 하는 데이터의 창고 — 회원 목록, 주문 내역 | 그 기록이 저장되는 곳 |

#### 프론트엔드를 이루는 3요소 — HTML · CSS · JS

> 방금 만든 그 화면은 사실 세 겹으로 되어 있습니다. 집 한 채를 짓는 순서와 똑같습니다 — 골조를 세우고, 인테리어를 하고, 설비를 넣습니다.

- HTML — 웹사이트의 뼈대·구조를 정의하는 문서 언어. "이건 제목, 이건 문단, 이건 버튼"처럼 정보에 의미를 부여해서, 검색엔진(구글·네이버)이 페이지에 무슨 내용이 있는지 알아보게 한다. 아주 예전 웹사이트들은 CSS 없이 HTML만으로 만들어졌다 — 밋밋하지만 내용은 다 담겨 있었다. → 집으로 치면 골조(뼈대)
- CSS — 그 뼈대에 옷을 입히는 것. 색·글꼴·크기·간격·배치 같은 스타일을 담당한다. 내용은 그대로 두고 겉모습만 바꾼다. → 집으로 치면 인테리어·도색
- JS(자바스크립트) — 웹사이트에 기능과 동작을 넣는다. 버튼을 누르면 반응하고, 화면이 움직이고, 입력을 받아 처리하는 등 사용자와 상호작용하는 부분을 맡는다. → 집으로 치면 전기·수도 같은 작동 설비

> 💡 [캡처: 같은 페이지의 3단계 비교 — ① HTML만 (검은 글자만 있는 밋밋한 화면) vs ② CSS 적용 (색·배치가 입혀진 화면) vs ③ JS까지 (버튼이 눌리고 화면이 바뀌는 화면)]

| 요소 | 맡은 역할 | 집짓기에 비유하면 |
| --- | --- | --- |
| **HTML** | 뼈대·구조 — 무엇이 담겨 있는지, 정보에 의미 부여 | 🏗️ 골조 |
| **CSS** | 스타일 — 색·글꼴·배치 등 겉모습 | 🎨 인테리어·도색 |
| **JS** | 기능·동작 — 클릭·움직임 등 사용자와의 상호작용 | 🔌 전기·수도 설비 |

**EXAMPLE PROMPT (① 뼈대만 — HTML)**
```
내 자기소개 웹사이트를 HTML만으로(CSS·JS 없이) 담백하게 만들어줘. 이름, 한 줄 소개, 좋아하는 것 3가지, 연락처만 넣어줘.
```

**EXAMPLE PROMPT (② 옷 입히기 — CSS)**
```
내용은 그대로 두고, 여기에 CSS만 더해서 더 멋지게 꾸며줘. 색·글꼴·여백을 넣어 보기 좋게.
```

**EXAMPLE PROMPT (③ 움직임 넣기 — JS)**
```
이제 다크모드/라이트모드 전환 버튼과 그 기능을 추가해줘. 버튼을 누르면 화면 색이 바뀌게.
```

- 같은 페이지를 세 번에 나눠 시켜보면, HTML→CSS→JS가 각각 무엇을 바꾸는지 눈으로 체감된다 — 뼈대만 있던 화면이 예뻐지고, 마지막엔 버튼이 실제로 작동한다

#### 데이터베이스는 사실 엑셀과 같습니다

| 엑셀에서 | 데이터베이스에서 |
| --- | --- |
| 파일 (.xlsx) | **데이터베이스** |
| 시트 | **테이블** |
| 행 (가로 한 줄) | **레코드** — 데이터 한 건 |
| 열 (세로 한 칸) | **컬럼** — 항목 이름 |

- 이름의 뜻 — HTML(HyperText Markup Language, 문서의 구조를 적는 언어) · CSS(Cascading Style Sheets, 색·모양을 정하는 규칙) · JS(JavaScript, 움직임을 만드는 프로그래밍 언어) · DB(Database, 데이터베이스)
- 방금 만든 페이지가 바로 프론트엔드다 — 오늘 만드는 것들은 전부 프론트엔드만으로 완성된다
- 백엔드와 DB가 "필요해지는 순간"은 Day 2에 직접 만나게 된다

#### "저장"은 어떻게 이루어지나 — 왕복 4단계

- 쇼핑몰에서 "주문하기"를 누르면: ① 화면이 서버에 요청 → ② 서버가 DB에 기록 → ③ DB가 결과를 돌려줌 → ④ 서버가 화면에 "완료" 알림. 웹 서비스 대부분이 이 왕복의 반복이다
- 반대로, 서버·DB 없이 화면에만 저장하면? 새로고침하면 지워지는 칠판과 같다 — "저장이 안 돼요"는 고장이 아니라 구조가 그렇게 되어 있는 것. 이 구조를 알면 "저장 안 돼, 고쳐줘" 대신 "데이터가 저장되게 바꿔줘"라고 짚어 말할 수 있다

#### 코드를 읽는 눈 — 딱 4단어

목표는 코드를 쓰는 게 아니라, Claude의 계획·보고를 **알아듣는 것**. 이 4단어면 충분하다.

| 단어 | 쉬운 뜻 | 예 |
| --- | --- | --- |
| **변수** | 값에 붙인 이름표 | `price = 3000` — '가격'을 부르는 방법 |
| **조건문** | "만약 ~라면" | 점수가 100이면 → "축하" 표시 (if) |
| **반복문** | "각각에 대해" | 상품 100개를 하나씩 화면에 그리기 (for) |
| **함수** | 이름 붙인 작업 묶음 | "점수계산()" — 자주 하는 일을 한 단어로 |

- 읽는 눈이 생기면 지시가 달라진다 — "그 부분 좀 고쳐줘" 대신 "조건문의 기준 금액을 5만 원으로 바꿔줘"

**실습 — 내 페이지 해부하기**

1. 브라우저에서 F12 키(노트북은 Fn+F12)를 눌러 개발자도구를 연다. 위쪽의 Elements(요소) 탭에서 내가 쓴 문장을 찾아보자 — 그게 바로 HTML이다. 오른쪽 Styles 영역에 보이는 것이 CSS다
2. Claude에게 물어본다: "이 페이지에서 프론트엔드는 어디까지야? 백엔드가 필요해지는 기능 3개를 제안해줘"
3. 이어서 시켜본다: "내 페이지 코드에서 변수·조건문·함수를 하나씩 찾아서, 무슨 일을 하는지 비개발자에게 설명하듯 한글로 번역해줘"
4. Claude가 "이 페이지에는 그런 코드가 없다"고 하면 당황하지 말자 — 첫 페이지는 단순해서 그럴 수 있다. "그럼 간단한 예시를 하나 추가해서 보여줘"라고 이어서 시키면 된다

#### 이 장을 마치면

- [ ] 프론트엔드·백엔드·데이터베이스를 식당에 비유해서 설명할 수 있다
- [ ] F12를 눌러 내 페이지의 HTML을 직접 눈으로 찾아봤다

#### 더 깊이 — 공식 자료

**참고 자료**

- [웹은 어떻게 동작하는가 (MDN, 한국어)](https://developer.mozilla.org/ko/docs/Learn_web_development/Getting_started/Web_standards/How_the_web_works) — developer.mozilla.org
- [JavaScript 첫걸음 (MDN, 한국어)](https://developer.mozilla.org/ko/docs/Learn_web_development/Core/Scripting) — developer.mozilla.org

---

### 프롬프트 엔지니어링 + 토큰 절약법

_DAY 1 · 11:10–12:00 · 이론 · 실습_

> 같은 AI라도 시키는 사람에 따라 결과가 완전히 달라집니다. 잘 시키는 법을 체화합니다.

> (도식: 좋은 프롬프트의 4요소(PTCF) 도식)

#### 좋은 프롬프트의 4요소 — PTCF(역할·할 일·맥락·형식)

| 요소 | 스스로 묻는 질문 | 예시 |
| --- | --- | --- |
| **P 역할** | 누구로서 만들게 하나? | "너는 소개 페이지를 잘 만드는 웹 디자이너야" |
| **T 할 일** | 무엇을 만드나? | "한 장짜리 소개 페이지를 만들어줘" |
| **C 맥락** | 대상·목적·꼭 지킬 조건은? | "보는 사람은 채용 담당자 / 갈색·크림색만 / 스크롤 없이 한 화면" |
| **F 형식** | 어떤 모양·산출물로? | "index.html 한 파일, 완성되면 브라우저로 열어줘" |

여기서 **'무엇이 성공인가(목표)'와 '꼭 지킬 조건(제약)'은 모두 C(맥락)에 담습니다** — 이 둘을 맥락에 넣는 것이 결과를 가장 크게 바꿉니다.

- 모호함은 비용이다 — "예쁘게 해줘"라고 하면 AI도 찍기를 한다. "카드형 레이아웃, 여백 넉넉히, 파란 계열"처럼 구체적으로 말하면 한 번에 원하는 게 나온다
- 토큰 = AI가 글을 읽고 쓸 때 세는 글자 조각 단위. 대화가 길어질수록 토큰이 쌓여서 느려지고, 사용량이 늘고, AI가 앞부분을 잊기 시작한다
- 절약 습관 3가지 — ① 주제가 바뀌면 /clear로 새 대화 시작 ② 관련 있는 파일만 보여주기 ③ 한 번에 하나씩 시키기

#### 왜 구체적이어야 하나 — "비워둔 칸은 평균으로 채워진다"

- AI는 마음을 읽지 못한다. 내가 말하지 않은 부분은 세상의 평균으로 채운다 — "홈페이지 만들어줘"라고 하면 세상 홈페이지의 평균이 나오는 이유
- 그래서 큰 것을 시킬 땐 첫 줄에 "단계적으로 개발할 예정이야"를 선언하고, 지금 할 일 하나만 말한다. 이 한 줄이 없으면 AI는 "완성 주문"으로 받아 한 번에 다 만들어버린다
- 첫 결과가 마음에 안 드는 건 정상이다. 처음부터 다시 시키지 말고, 아쉬운 부분만 짚어서 다시 시킨다 — "대표 메뉴만 바꿔줘. 나머지는 그대로"

#### 외워둘 만능 프롬프트 6개

| 상황 | 이렇게 말한다 |
| --- | --- |
| 에러가 났을 때 | "에러 나는데 고쳐줘" + **[에러 화면 스크린샷]** |
| 방금 것이 마음에 안 들 때 | "방금 한 거 되돌려줘" (같은 대화 안에서만 통한다) |
| 새 대화를 시작한 직후 | "지금 이 프로젝트 구조랑 현재 상태 정리해줘" |
| 다음 작업으로 넘어가기 전 | "지금 페이지 전체 한번 점검해줘. 깨진 거 없는지" |
| 대화가 길어져 느려질 때 | `/compact` (요약하고 이어가기) |
| 큰 것을 만들기 시작할 때 | "단계적으로 개발할 예정이야. 지금은 ○○만 만들어줘" |

**실습 — 나쁜 프롬프트 vs 좋은 프롬프트**

1. 1차: "자기소개 페이지 예쁘게 만들어줘"로 요청해 결과를 본다
2. 2차: 아래처럼 PTCF(역할·할 일·맥락·형식)를 갖춘 프롬프트로 다시 요청한다
3. 두 결과를 나란히 놓고 비교한다 — 어떤 요소가 결과를 가장 크게 바꿨는지 찾아보자

**EXAMPLE PROMPT (2차)**
```
너는 카페 소개 페이지를 잘 만드는 웹 디자이너야. [P 역할]
따뜻한 동네 카페를 알리는 소개 페이지를 만들어줘. [T 할 일]
보는 사람이 3초 안에 "따뜻한 동네 카페" 인상을 받게 / 스크롤 없이 한 화면 / 갈색·크림색 계열만. [C 맥락]
index.html 한 파일로, 상단 로고 텍스트·중앙 한 줄 소개·하단 인스타 링크 버튼 구성. [F 형식]
```

#### 더 깊이 — 공식 자료

**참고 자료**

- [프롬프트 엔지니어링 공식 문서 (Anthropic)](https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview) — docs.anthropic.com
- [구글 공식 프롬프트 가이드 — 우리가 쓰는 PTCF의 출처](https://blog.google/products-and-platforms/products/workspace/google-gemini-workspace-ai-prompt-tips/) — blog.google
- [Claude Code 활용 베스트 프랙티스](https://www.anthropic.com/engineering/claude-code-best-practices) — anthropic.com

---

### 바이브코딩 플로우 + 깃(Git)

_DAY 1 · 13:00–13:30 · 이론_

> 오후 프로젝트에서 그대로 밟을 3단계 플로우와, 내 결과물을 인터넷에 공개하는 수단(깃)을 배웁니다.

#### 모든 프로젝트가 밟는 3단계

| 단계 | 하는 일 | 한 줄 요령 |
| --- | --- | --- |
| **① 기획·디자인** | 무엇을 왜 만들지 정한다 | "누가 보나 / 무엇을 보여주나 / 어떤 분위기인가" 세 가지만 정해도 프롬프트가 저절로 좋아진다 |
| **② 구현·디버깅** | 만들고 고친다 | 오전에 배운 프롬프트 기술을 여기서 쓴다 — 섹션 하나씩, 한 번에 하나만 |
| **③ 배포** | 인터넷에 공개한다 | 내 컴퓨터에서만 열리던 파일이 전 세계에서 열리는 URL이 된다 |

#### 배포는 어떻게 이루어지나

> (도식: GitHub Pages 배포 흐름 도식)

#### 깃(Git) — 겁먹지 않아도 되는 이유

- 깃(Git) = 코드의 세이브 포인트. 게임처럼 "여기까지 저장"을 남겨두고, 망치면 그 지점으로 되돌린다
- 왜 Ctrl+S로는 부족한가 — ① 어제 상태로는 못 돌아간다(Ctrl+Z의 한계) ② 컴퓨터가 고장 나면 코드가 사라진다 ③ 배포하려면 코드가 먼저 클라우드에 올라가 있어야 한다

#### 용어 4개 — 택배로 기억하기

| 용어 | 뜻 | 택배 비유 |
| --- | --- | --- |
| **저장소** | 내 프로젝트가 사는 방 (GitHub 위) | 택배 상자 |
| **커밋** | 저장 지점 만들기 | 포장하고 송장 붙이기 |
| **푸시** | 클라우드(GitHub)에 올리기 | 발송 |
| **배포** | 누구나 볼 수 있게 공개 | 배달 완료 — 받는 사람이 열어봄 |

- 깃허브(GitHub) = 코드를 올려두는 클라우드. 코드계의 구글드라이브라고 생각하면 된다
- GitHub Pages = 깃허브에 올린 HTML을 공짜로 진짜 웹사이트로 만들어주는 기능. 오늘 우리의 배포 수단
- Claude가 "커밋하겠습니다"라고 말하면 = "포장해서 저장 지점을 만들겠다"는 뜻이다

> 💡 명령어를 외울 필요 없습니다 — 오늘은 웹사이트에서 마우스 클릭만으로 배포하고, 익숙해지면 Claude에게 **"이 프로젝트 GitHub에 올려줘"** 한 마디면 저장소 생성부터 푸시까지 전부 해줍니다. 우리는 뜻만 알아들으면 됩니다.

#### 깃 vs 깃허브 — 헷갈리지 않게

> 이름이 비슷해서 자주 헷갈립니다. 둘은 짝꿍이지만 역할이 다릅니다 — 하나는 **내 컴퓨터의 기능**, 하나는 **인터넷 위의 창고**입니다.

|  | 깃(Git) | 깃허브(GitHub) |
| --- | --- | --- |
| **정체** | 내 컴퓨터에서 **세이브 지점을 관리하는 도구** | 그 세이브 지점을 **인터넷(클라우드)에 백업·공유**하는 저장소 서비스 |
| **어디에 있나** | 내 컴퓨터 안 | 인터넷(웹사이트) |
| **게임 비유** | 게임의 **저장 기능** 그 자체 | **클라우드 세이브** — 다른 기기에서도 불러오고 친구와 공유 |
| **없으면** | 되돌리기가 안 됨 | 내 컴퓨터가 고장 나면 코드가 사라짐 |

- 즉 깃으로 세이브(커밋)하고 → 깃허브로 올린다(푸시). 이 두 단계가 한 세트다
- 핵심 가치는 안도감 — 실험하다 망가져도 저장 지점으로 언제든 되돌릴 수 있다. 그래서 겁내지 않고 이것저것 시도할 수 있다

#### 명령 두 개만 더 — 클론과 풀

> 앞의 택배 표에서 **커밋(포장)·푸시(발송)**는 이미 봤습니다. 반대로 **가져오는** 쪽 두 개만 더 알면 끝입니다.

| 용어 | 뜻 | 비유 |
| --- | --- | --- |
| **커밋(commit)** | 지금 상태로 세이브 지점 만들기 (앞 표 복습) | 물건 포장하고 송장 붙이기 |
| **푸시(push)** | 세이브 지점을 클라우드(깃허브)에 올리기 (앞 표 복습) | 택배 발송 |
| **클론(clone)** | 남의(또는 내) 저장소를 통째로 내려받기 | 남의 상자를 통째로 복제해 내 책상에 놓기 |
| **풀(pull)** | 클라우드의 최신 내용만 가져와 내 것에 반영 | 바뀐 부분만 새로 배달받기 |

#### 비개발자는 명령을 외울 필요가 없다

- 위 용어들은 Claude가 하는 말을 알아듣기 위한 것이지, 내가 타이핑하려고 외우는 게 아니다
- 실제로는 말로 시키면 Claude가 대신 처리한다. 아래처럼 부탁하면 된다

**EXAMPLE PROMPT (깃 시작하기)**
```
이 폴더에 깃을 초기화해줘. 앞으로 여기서 저장 지점을 만들 수 있게.
```

**EXAMPLE PROMPT (세이브 = 커밋)**
```
지금까지 만든 걸 커밋해줘. 커밋 메시지는 지금 상태에 맞게 적당히 지어줘.
```

**EXAMPLE PROMPT (세이브 목록 보기)**
```
지금까지의 커밋 이력을 보기 쉽게 보여줘.
```

**EXAMPLE PROMPT (되돌리기)**
```
방금 것 말고, 그 이전 커밋 상태로 되돌려줘.
```

> 💡 커밋은 **자주** 하는 게 좋습니다 — 뭔가 **제대로 작동하는 지점마다** 세이브하세요. 그래야 나중에 망가졌을 때 **가장 가까운 멀쩡한 지점**으로 돌아올 수 있습니다. "잘 되던 순간"을 그때그때 붙잡아 두는 것이 핵심입니다. 그리고 깃허브를 쓰려면 **GitHub 계정**이 필요합니다 — **준비물 안내**를 참고해 미리 가입해 두세요(무료).

#### 더 깊이 — 공식 자료

**참고 자료**

- [GitHub Pages란? (공식 문서, 한국어)](https://docs.github.com/ko/pages/getting-started-with-github-pages/what-is-github-pages) — docs.github.com
- [Git 공식 사이트 (다운로드)](https://git-scm.com) — git-scm.com

---

### 나만의 프로필 페이지 — 만들고, 세상에 공개하기

_DAY 1 · 13:30–15:00 · 프로젝트 1_

> 기획 → 구현 → 배포의 풀사이클을 처음부터 끝까지. 끝나면 세상 어디서든 열리는 내 URL이 생깁니다.

#### 진행 순서

| 단계 | 시간 | 하는 일 |
| --- | --- | --- |
| **① 기획** | 15분 | 바로 만들지 말고, 아래 프롬프트처럼 구성부터 Claude와 함께 정한다 |
| **② 구현** | 40분 | 정한 목록을 섹션 하나씩 요청 — "전부 다 만들어줘"보다 "먼저 소개 섹션만"이 결과가 좋다 |
| **③ 다듬기** | 15분 | 마음에 안 드는 부분을 구체적인 말로 — "글자가 너무 빽빽해, 줄 간격을 넓혀줘" |
| **④ 배포** | 20분 | 아래 배포 순서를 그대로 따라간다 |

**EXAMPLE PROMPT (기획)**
```
나를 소개하는 프로필 페이지를 만들고 싶어. 만들기 전에 먼저,
어떤 섹션(소개, 경력, 취미, 연락처 등)으로 구성하면 좋을지
목록으로 제안해줘. 분위기는 "차분하고 신뢰감 있게".
```

구현 단계에서는 아래 틀을 쓰세요. **[대괄호] 부분만 내 것으로 바꾸면** 됩니다 — 앞서 배운 PTCF가 그대로 들어가 있습니다.

**EXAMPLE PROMPT (구현 — 빈칸만 채우기)**
```
P(역할): 너는 개인 소개 페이지를 잘 만드는 웹 디자이너야.
T(할 일): [내 이름]을 소개하는 한 장짜리 페이지를 만들어줘.
C(맥락):
- 보는 사람: [친구/채용 담당자/잠재 고객]
- 목적: [나를 알리기/포트폴리오/연락 받기]
- 구성: 큰 제목 + 한 줄 소개 → 특징 3가지 → 연락 방법
- 분위기: [차분한/밝은/전문적인] 톤, 포인트 색은 [좋아하는 색]
F(형식):
- index.html 파일 하나로 (HTML+CSS+JS만, 다른 도구 금지)
- 휴대폰에서도 안 깨지게
- 완성되면 브라우저에서 바로 열어줘
```

**배포 순서 — GitHub Pages (클릭만으로 끝)**

1. 배포 전 확인 2가지 — 저장소는 Public으로 만들 것(무료 요금제는 공개 저장소만 Pages 가능), 첫 화면 파일명은 정확히 `index.html`일 것(Index.html, home.html은 안 됨)
2. github.com 로그인 → 우상단 + → New repository → 이름 입력(영문 소문자로 짓자, 예: profile) → Create repository
3. uploading an existing file 링크를 눌러 프로젝트 폴더 안의 파일을 전부 드래그해서 올리고 Commit changes — 파일이 index.html 하나뿐이어도 충분하고, Claude가 CSS나 이미지 파일을 따로 만들었다면 그것들도 빠짐없이 함께 올린다
4. 저장소 상단 Settings → Pages 메뉴 → Source는 Deploy from a branch, Branch를 main + / (root)로 선택하고 Save
5. 잠시 후 새로고침하면 "Your site is live at…" 상자가 뜬다 — 주소 규칙은 https://아이디.github.io/저장소이름/
6. 내 폰으로도 열어본다 — 와이파이가 아니어도 열린다. 진짜 인터넷에 있다는 뜻이다

#### 이 장을 마치면

- [ ] 내 프로필 페이지가 https://아이디.github.io/… 주소로 열린다
- [ ] 폰으로도 열어봤다 — 진짜 인터넷 위에 있다는 뜻이다
- [ ] 옆 사람과 URL을 주고받아 서로의 페이지를 구경했다

> 💡 이후 수정은 쉽습니다 — 저장소에서 **Add file → Upload files**로 **같은 이름의 파일을 다시 올리면 자동으로 덮어쓰기**되고, 1~2분 뒤 사이트에 반영됩니다. 첫 배포만 최대 10분 걸릴 수 있어요.

#### 잘 안 될 때

| 증상 | 해결 |
| --- | --- |
| 404 페이지가 뜸 | 첫 화면 파일명이 `index.html`이 아님 → Claude에게: **"첫 화면 파일 이름을 index.html로 바꿔줘"** |
| 고쳤는데 그대로임 | 몇 분 기다린 뒤 **Ctrl+F5** 강력 새로고침 |
| Pages 메뉴가 안 켜짐 | 저장소가 Private — Settings → General에서 Public으로 변경 |
| 화면은 뜨는데 모양이 깨짐 | Claude에게: **"CSS 파일 경로가 맞는지 확인해줘. 대소문자까지"** |

#### 더 깊이 — 공식 자료

**참고 자료**

- [GitHub Pages 퀵스타트 (공식, 한국어)](https://docs.github.com/ko/pages/quickstart) — docs.github.com
- [Pages 사이트 만들기 단계별 안내](https://docs.github.com/ko/pages/getting-started-with-github-pages/creating-a-github-pages-site) — docs.github.com

---

### 고전게임 만들기 — 디버깅 사이클 체득

_DAY 1 · 15:10–16:40 · 프로젝트 2_

> 테트리스 · 뱀게임 · 벽돌깨기 중 하나를 만듭니다. 목표는 완성이 아니라 **버그를 만나고 고치는 경험**입니다.

> (도식: 디버깅 순환 도식)

#### 에러를 만나면 이 순서로 — F12 → Console → 캡처

- 브라우저에서 F12 → Console 탭 → 빨간 글씨를 찾는다. 개발자도구를 마스터할 필요 없다 — 이것만 기억하면 된다
- 최고의 에러 신고는 의외로 간단하다: Win+Shift+S로 에러 화면을 캡처해서 붙여넣고 "이 에러 고쳐줘" — Claude는 이미지에서 에러 메시지를 정확히 읽어낸다
- 에러 메시지가 안 보이는데 이상할 때는 — 뭘 했고 / 뭐가 나왔고 / 뭘 원하는지 세 가지를 말한다 (아래 템플릿)

#### 디버깅 3원칙 — 3일 내내 쓰게 된다

| 원칙 | 이렇게 한다 | 왜 |
| --- | --- | --- |
| **① 에러는 그대로** | 복사 또는 캡처로 전문 전달 — 요약·말로 옮기기 금지 | 에러 전문이 최고의 단서다 |
| **② 기대 vs 실제** | "안 돼요" 대신 "기대한 것은 A인데 실제로는 B" (아래 템플릿) | AI가 차이를 정확히 좁힐 수 있다 |
| **③ 한 번에 하나** | 버그 하나 고치고 확인, 그다음 하나 | 동시에 고치면 뭐가 문제였는지 알 수 없다 |
| **+ 3번 실패하면** | "다른 방식으로 해결해줘" 또는 그 기능만 처음부터 다시 | 같은 길을 반복하면 같은 곳에 도착한다 |

> 💡 **마지막 탈출구 — 새 폴더에서 다시 시작해도 됩니다.**
> 여러 번 고쳤는데 점점 더 꼬인다면, 고치는 것보다 **새로 만드는 게 빠를 때가 있습니다.** 새 폴더를 만들고 지금까지 배운 걸로 다시 시키세요 — 두 번째는 훨씬 빠릅니다. 망가진 프로젝트를 붙들고 있는 것보다 훨씬 나은 선택이고, 실패가 아닙니다.

#### 같은 "게임 만들어줘"라도 — 지시가 구체적일수록 결과가 좋아진다

> 이번 실습은 **사과게임**으로 "구체적으로 지시하는 연습"을 합니다. 완성이 목적이 아니라, **규칙을 얼마나 또박또박 설명하느냐**에 따라 결과가 어떻게 달라지는지 몸으로 느끼는 게 목적입니다.

- 그냥 "게임 만들어줘"라고 하면 AI는 빈칸을 자기 마음대로 채웁니다 — 내가 상상한 게임과 전혀 다른 게 나오기 쉽습니다
- 반대로 규칙·화면·조작법을 구체적으로 풀어주면, AI는 내 머릿속에 훨씬 가깝게 만들어줍니다. 이게 바이브코딩에서 가장 중요한 기술입니다
- 그래서 오늘은 내가 원하는 것을 말로 정확히 풀어내는 연습을 사과게임으로 합니다

#### 사과게임이란 — 이 규칙을 그대로 AI에게 설명하게 됩니다

- 화면에 1~9 숫자가 적힌 사과들이 직사각형 격자(줄과 칸)로 빽빽하게 놓여 있습니다
- 플레이어는 마우스로 드래그해서 사각형 범위를 지정해 그 안의 사과들을 한꺼번에 선택합니다
- 선택한 사과 숫자의 합이 정확히 10이면 그 사과들이 사라지고 점수가 오릅니다 (10이 아니면 아무 일도 없음)
- 제한 시간(예: 2분) 안에 최대한 많은 사과를 없애는 것이 목표입니다
- 실제로 어떤 게임인지 먼저 직접 해보면 규칙 설명이 훨씬 쉬워집니다 → gamesaien.com 사과게임(Fruit Box)

> 💡 **먼저 진짜 게임을 3분만 해보세요.** 직접 플레이해 본 게임은 규칙을 설명하기가 훨씬 쉽습니다. "드래그하면 어떻게 되는지", "합이 10일 때 무슨 일이 일어나는지"를 눈으로 확인한 뒤 AI에게 설명하면 결과물의 완성도가 확 올라갑니다.

**실습 — 사과게임 만들기**

1. 먼저 진짜 사과게임을 잠깐 해보며 규칙을 눈에 익힌다
2. 바로 "만들어줘"가 아니라, 아래처럼 "계획부터 세워줘"라고 시킨다 — AI가 세운 계획을 읽어보면 내 설명에서 빠진 부분이 보인다
3. 계획이 마음에 들면 "이 계획대로 만들어줘"라고 이어서 시킨다
4. 완성되면 진짜 사과게임과 나란히 켜고, 다른 점을 하나씩 짚어 개선을 요청한다
5. 버그를 만나면 아래 버그 신고 템플릿으로 신고한다

**EXAMPLE PROMPT (계획부터 — plan-first)**
```
사과 게임을 만들 계획을 세워보자. 내가 원하는 건 https://www.gamesaien.com/game/fruit_box_a 와 같은 게임이야.

규칙은 이래:
- 화면에 1~9 숫자가 적힌 사과들이 직사각형 격자로 빽빽하게 놓여 있어.
- 마우스로 드래그해서 사각형 범위를 지정하면, 그 안의 사과들이 선택돼.
- 선택한 사과 숫자의 합이 정확히 10이면 그 사과들이 사라지고 점수가 올라가. 10이 아니면 아무 일도 없어.
- 제한 시간 2분 안에 최대한 많은 사과를 없애는 게 목표야.

사과는 별도 이미지 없이, 원 배경 위에 1~9 숫자를 그려서 표현해줘(이미지 파일 준비 안 해도 되게).
완성되면 index.html 파일 하나로 만들어서, 더블클릭해 브라우저로 바로 열 수 있게 해줘.
일단 코드부터 짜지 말고, 계획부터 세워줘.
```

> 💡 **왜 "계획부터"일까?** 바로 만들게 하면 잘못된 방향으로 한참 가버립니다. 계획을 먼저 받아 읽어보면 **내 설명에서 빠진 규칙**(예: 시간이 끝나면 어떻게 되는지)이 눈에 보이고, 코드가 나오기 전에 바로잡을 수 있습니다. 계획이 이상하면 "이 부분은 이렇게 바꿔줘"라고 고친 뒤 만들게 하세요.

**EXAMPLE PROMPT (최종 산출물 형태 — 실행 방법 지정)**
```
완성되면 index.html 파일 하나로 만들어줘.
따로 서버를 켜지 않아도, 그 파일을 더블클릭하면 브라우저에서 바로 열려서 플레이할 수 있게 해줘.
```

**EXAMPLE PROMPT (버그 신고 템플릿)**
```
버그가 있어.
기대한 것: 합이 10인 사과를 드래그하면 사라져야 해.
실제 동작: 드래그해도 아무 반응이 없어.
에러 메시지: (F12 콘솔에 뜬 빨간 글씨를 그대로 붙여넣기)
이것만 고쳐줘. 다른 부분은 건드리지 마.
```

#### 진짜 게임과 비교하며 다듬기 — "구현 안 된 부분"을 하나씩 짚기

내가 만든 게임과 [진짜 사과게임](https://www.gamesaien.com/game/fruit_box_a)을 창 두 개로 나란히 켜세요. 그리고 **"진짜는 이런데 내 건 이래"** 식으로 다른 점을 **하나씩** 짚어 고쳐 나갑니다. 한 번에 하나씩이 핵심입니다.

**세부 튜닝 프롬프트 예시**

1. 드래그하는 동안 선택 영역이 눈에 보이지 않는다 → 미리보기 추가 요청
2. 사과가 너무 적거나 크다 → 개수·크기 조정 요청
3. 합이 10이 되어 사과가 사라진 자리에 새 사과가 채워진다 → 원래 규칙대로 안 채우게 요청

**EXAMPLE PROMPT (드래그 영역 미리보기)**
```
지금은 마우스로 드래그해도 어디를 고르는지 안 보여.
진짜 사과게임처럼, 드래그하는 동안 선택 중인 사각형 영역이 반투명한 박스로 화면에 보이게 해줘.
이 부분만 고치고 다른 건 건드리지 마.
```

**EXAMPLE PROMPT (사과 개수·크기 조정)**
```
지금 사과가 너무 크고 개수가 적어서 게임이 싱거워.
진짜 게임처럼 가로 17칸, 세로 10칸 정도로 사과를 촘촘하게 배치하고, 사과 크기도 그에 맞게 줄여줘.
이 부분만 고쳐줘.
```

**EXAMPLE PROMPT (사라진 자리 안 채우기 + 드래그 다듬기)**
```
두 가지만 고쳐줘.
1) 합이 10이 되어 사과가 사라진 자리에 새 사과가 다시 채워지고 있어. 원래 규칙대로, 사라진 자리는 그냥 빈칸으로 둬.
2) 드래그가 어색해 — 마우스를 살짝만 움직여도 뚝뚝 끊겨. 부드럽게 이어지도록 다듬어줘.
한 번에 하나씩 확인할 거니까, 1번부터 고쳐주고 결과를 알려줘.
```

#### 개발자 도구 아주 살짝 — "이 부분이 이상해"를 정확히 짚는 법

- 브라우저에서 F12를 누르면 개발자 도구가 열립니다. 마스터할 필요 전혀 없고, 딱 하나만 기억하세요
- 개발자 도구 왼쪽 위의 요소 선택기(화살표 아이콘)를 누른 뒤 화면의 특정 부분을 클릭하면, 그 부분이 어떤 요소인지 정보가 표시됩니다
- Claude에게 "이 부분이 이상해"라고 말할 때, 그냥 말로 하기보다 개발자 도구로 그 부분을 짚어 확인한 뒤 설명하면 훨씬 정확하게 전달됩니다 (또는 Win+Shift+S로 그 화면을 캡처해 붙여넣기)

> 💡 [캡처: 크롬 개발자 도구(F12) — 왼쪽 위 요소 선택기(화살표 아이콘)로 화면의 사과 하나를 클릭해 정보가 뜬 화면]

#### 이 장을 마치면

- [ ] 내 게임이 실제로 플레이된다 — 완성도는 아직 중요하지 않다
- [ ] 버그를 최소 한 번 만나서, 템플릿으로 신고해 고쳐봤다
- [ ] "기대한 것 vs 실제 동작"으로 말하는 요령이 입에 붙기 시작했다

#### 더 깊이 — 공식 자료

**참고 자료**

- [브라우저 개발자도구란? (MDN)](https://developer.mozilla.org/en-US/docs/Learn_web_development/Howto/Tools_and_setup/What_are_browser_developer_tools) — developer.mozilla.org
- [Console 사용법 (Chrome 공식 문서)](https://developer.chrome.com/docs/devtools/console) — developer.chrome.com

---

### Day 1 회고

_DAY 1 · 16:40–17:00 · 회고_

> 오늘 만든 것을 자랑하고, 막혔던 지점을 함께 정리하며 하루를 닫습니다.

#### 함께 하기

- 배포 URL 릴레이 — 서로의 프로필 페이지를 방문해보자. 오늘 아침만 해도 없던 것들이다
- 오늘 가장 크게 막혔던 지점과 해결법 공유 — 남의 막힘이 내일의 내 해결책이 된다

#### 스스로 점검 — 3개에 답할 수 있으면 오늘은 성공

- 프론트엔드 / 백엔드 / 데이터베이스는 각각 무슨 일을 하나?
- 좋은 프롬프트의 4요소(PTCF)는?
- "배포"란 무엇이고, 나는 오늘 어떻게 배포했나?

> 💡 Day 2 예고 — 오늘까지는 "내가 쓴 내용"만 화면에 띄웠습니다. 내일은 날씨·유튜브 같은 **바깥 세상의 데이터**를 내 서비스로 끌어옵니다 (API).

## DAY 2 — 연결

---

### 연결 — API로 "진짜 서비스"를 만든다

_DAY 2_

> 오늘의 목표: 외부 데이터·기능(API)을 연결하고, 웹을 넘어 크롬 익스텐션 형태까지 만들어본다.

> 오늘의 산출물 — **크롬에 설치된 익스텐션 1개 + 데이터 시각화 AI 서비스 1개 + 테스트 결제 성공 경험**

어제는 "내가 쓴 내용"만 화면에 띄웠다면, 오늘은 **바깥 세상의 살아있는 데이터**를 내 서비스로 끌어옵니다. 오전에 API 개념을 잡고 크롬 익스텐션을 만들며, 오후에는 공공 데이터 서비스와 결제 연동까지 경험합니다.

#### 시간표

| 시간 | 구분 | 세션 |
| --- | --- | --- |
| 09:00–09:30 | 복습 | [Day 1 복습 퀴즈](#d2-1) |
| 09:30–10:10 | 이론+실습 | [CLAUDE.md와 Skill](#d2-2) |
| 10:10–10:40 | 이론 | [API란 무엇인가](#d2-3) |
| 10:40–11:00 | 완충 | [랜덤 이미지 API 미술관](#d2-g) |
| 11:00–12:00 | 프로젝트 | [프로젝트 3 · 구글 API 크롬 익스텐션](#d2-4) |
| 13:00–14:50 | 프로젝트 | [프로젝트 4 · 공공 API 시각화 + AI](#d2-5) |
| 15:00–16:40 | 따라하기 | [쇼핑몰 + 결제 연결](#d2-6) |
| 16:40–17:00 | 회고 | [Day 2 회고](#d2-7) |

---

### Day 1 복습 퀴즈

_DAY 2 · 09:00–09:30 · 복습_

> 어제 배운 내용을 간단히 복습하고, 막힌 부분을 해소한 뒤 시작합니다.

#### 스스로 점검 — 답해보고 넘어가자

- 프론트엔드 / 백엔드 / 데이터베이스는 각각 무슨 일을 하나? (식당 비유로 답해도 좋다)
- 좋은 프롬프트의 4요소(PTCF)는? 어제 내 프롬프트에는 몇 개가 들어 있었나?
- "배포"란 무엇인가? 내 프로필 페이지 주소는 지금도 열리나? (지금 폰으로 확인해보자)
- 버그를 만났을 때 AI에게 어떻게 말해야 하나?

---

### CLAUDE.md와 Skill — AI에게 규칙을 기억시키기

_DAY 2 · 09:30–10:10 · 이론 · 실습_

> 어제 매번 반복했던 "한국어로 답해줘", "한 파일로 해줘" — 그 반복을 파일 하나로 없앱니다.

#### CLAUDE.md — 매일 아침 기억이 리셋되는 비서에게

- 클로드 코드는 새 대화마다 기억이 리셋되는 비서다 — 어제 정한 디자인을 잊고, 갑자기 영어로 답하기도 한다. 그래서 책상 위에 업무 지침서를 올려둔다: 그게 CLAUDE.md
- 프로젝트 폴더에 두면 대화를 시작할 때 자동으로 먼저 읽힌다 — 한 번 적으면 매번 말할 필요가 없고, /clear 후에도 계속 적용된다. 토큰 절약과도 궁합이 좋다
- 위치가 중요하다 — 반드시 프로젝트 맨 위(루트) 폴더에. 하위 폴더에 넣으면 못 찾는다
- 가장 쉬운 생성법은 /init — 클로드 코드가 프로젝트를 분석해 알아서 만들어준다. 그 위에 내 규칙을 보태면 된다
- 작성 팁 — ① 간결하게(길다고 잘 이해하는 게 아니다) ② 규칙은 구체적으로("예쁘게" ❌ → "파란 계열, 카드형 레이아웃" ⭕) ③ 처음부터 완벽할 필요 없이 점점 키운다
- 언제 업데이트하나 — 새 기능·새 규칙이 생겼을 때, 그리고 AI가 같은 실수를 반복할 때 그 실수를 규칙으로 적는다

#### Skill — 반복 작업을 명령어 하나로

- Skill = 자주 하는 작업 절차를 등록해두고 /이름 한 줄로 실행하는 기능. 엑셀 매크로와 비슷하다 — 예: /배포라고 치면 정해둔 배포 절차가 자동 실행
- 만드는 법도 말로 — "○○하는 스킬을 만들어줘"라고 시키면 된다. 파일을 직접 만들 필요 없다
- 스킬로 만들면 좋은 작업 — ① 반복되고 ② 순서가 정해져 있고 ③ 자주 하는 작업. "코드 잘 짜줘" 같은 막연한 건 스킬이 아니다

**실습 — 오늘 쓸 CLAUDE.md 만들기**

1. 바탕화면에 vibe-day2 폴더를 새로 만든다 — 어제 프로필 폴더가 아니라 오늘 실습용 새 폴더다
2. 그 안에 CLAUDE.md 파일을 만든다 (Claude에게 "만들어줘"라고 시켜도 된다)
3. 아래 예시를 참고해 내 규칙을 적는다
4. 새 대화를 시작해 아무 요청이나 해보고, 규칙이 자동으로 적용되는지 확인한다

**CLAUDE.md 예시**
```
# 프로젝트 규칙
- 답변은 한국어로, 설명은 초보자 눈높이로
- HTML/CSS/JS는 파일 하나에 모아서 작성
- 코드를 바꾸면 무엇을 왜 바꿨는지 한 줄로 알려주기
- API 키는 절대 코드에 직접 적지 않기
```

#### 이 장을 마치면

- [ ] 내 프로젝트 폴더에 CLAUDE.md가 들어 있다
- [ ] 새 대화를 열어도 내가 적은 규칙이 자동으로 지켜지는 걸 확인했다

#### 더 깊이 — 공식 자료

**참고 자료**

- [CLAUDE.md(메모리) 공식 문서](https://code.claude.com/docs/en/memory) — code.claude.com
- [Skill 공식 문서](https://code.claude.com/docs/en/skills) — code.claude.com

---

### API란 무엇인가

_DAY 2 · 10:10–10:40 · 이론_

> 오늘 하루 종일 쓰게 될 단 하나의 개념 — "정해진 형식으로 요청하면, 데이터가 온다."

> (도식: API 요청과 응답 도식)

#### 핵심 내용

- API = Application Programming Interface의 약자. "응용 프로그램끼리 대화하는 접속 창구"라는 뜻으로, 다른 회사의 데이터·기능을 정해진 형식으로 가져다 쓰는 통로다
- 흐름은 항상 같다: 요청("이 주소로 서울 미세먼지 주세요") → 응답("여기 있습니다"). 응답은 주로 JSON(JavaScript Object Notation의 약자)이라는 형식 — 중괄호 { }로 정리된 데이터 꾸러미인데, 읽고 해석하는 건 AI가 해주니 겁먹지 않아도 된다
- API 키 = "나는 요청할 자격이 있어요"를 증명하는 열쇠. 발급받은 사람 명의로 사용량이 기록된다
- 무료 API는 어디서 찾나 — 공공데이터포털(data.go.kr, 정부가 공개한 데이터), Google Cloud 콘솔(구글 서비스들)

#### ⚠️ 키는 비밀번호다 — 오늘 가장 중요한 안전 수칙

- 어제 깃허브에 올리는 법을 배웠기 때문에 오늘이 가장 위험한 날이다 — 키가 적힌 코드를 그대로 올리면, 남이 내 키로 마구 요청해서 요금 폭탄을 맞을 수 있다
- 안전 수칙 ① — Claude에게 "API 키가 코드에 노출되지 않게 해줘"라고 요청하기
- 안전 수칙 ② — 깃허브에 올리기 전, 키가 든 파일이 포함됐는지 반드시 확인하기
- "그런데 오늘 실습은 키를 페이지 코드에 넣던데요?" — 맞다. 오늘은 연습용 무료 키라서 코드에 넣고 배우되, 그 코드를 깃허브에 올리지 않는 것이 오늘의 규칙이다. 실제 서비스라면 키를 서버 쪽에 숨긴다 — 그게 백엔드가 필요한 또 하나의 이유다

#### 더 깊이 — 공식 자료

**참고 자료**

- [공공데이터포털 (무료 공공 API)](https://www.data.go.kr) — data.go.kr
- [Google Cloud 콘솔 (구글 API 키 발급)](https://console.cloud.google.com) — console.cloud.google.com

---

### 랜덤 이미지 API 미술관 — 키 없이 첫 성공

_DAY 2 · 10:40–11:00 · 완충 실습_

> 오늘 첫 성취를 먼저 챙깁니다 — **키 발급도 가입도 없이** API를 붙여 미술관 사이트를 완성해, "API로 데이터를 받아 화면에 뿌리는" 감을 손에 익힙니다. 이 감각 위에서 다음 세션부터 **키가 필요한 진짜 API**로 확장합니다.

#### 왜 이걸 먼저 하나 — 완충(warm-up)

- 곧 배울 구글 API·공공데이터 API는 회원가입, 키 발급, 사용 신청 같은 준비 단계가 많아 시작부터 막히기 쉽습니다 — 그 앞에서 지치면 정작 재미있는 부분에 도달하기도 전에 힘이 빠집니다
- 그래서 키가 전혀 필요 없는 API로 "API 붙이는 성공"을 먼저 한 번 맛봅니다. 성공 경험을 손에 쥐고 나면, 뒤에 나오는 준비 단계들이 훨씬 만만해집니다
- 핵심은 준비물이 아니라 흐름입니다 — 정해진 주소로 요청하면 데이터가 응답으로 오고, 그걸 화면에 뿌린다. 이 한 바퀴를 이번 실습에서 몸으로 겪어 둡니다

#### picsum.photos — 키가 필요 없는 API

- picsum.photos는 주소 하나만 부르면 랜덤 사진을 돌려주는 무료 이미지 API입니다. 회원가입도, API 키도 없습니다 — 주소가 곧 요청입니다
- 예를 들어 https://picsum.photos/300 이라고 요청하면 가로·세로 300픽셀짜리 랜덤 이미지 한 장이 옵니다. 주소 끝의 숫자가 이미지 크기이고, 부를 때마다 다른 사진이 나옵니다
- 브라우저 주소창에 https://picsum.photos/300을 그대로 쳐 보세요. 매번 새 사진이 뜨는 게 보이면, 그게 바로 API가 응답하는 순간입니다 — 어렵게 느껴지던 "API 호출"이 사실 이렇게 간단한 일이라는 걸 눈으로 확인하는 단계입니다

> 💡 주소 뒤에 조금씩 붙여 보면 API에 "옵션을 얹는" 감이 잡힙니다 — `/300/200`은 가로 300·세로 200, `/300?grayscale`은 흑백, `/300?blur`는 흐릿하게. 이렇게 **같은 API에 요청을 조금씩 바꿔 보는 것**이 내일 진짜 API를 다룰 때도 그대로 쓰이는 습관입니다.

#### 실습 — 가상 미술관 사이트 만들기

- 이 API로 사진 전시회처럼 보이는 웹사이트를 만듭니다. 랜덤 이미지 30장 정도를 격자(그리드)로 정렬해, 진짜 온라인 갤러리처럼 보이게 하는 게 목표입니다
- 지난 세션들처럼 계획부터 세우게 하는(plan-first) 프롬프트로 시작합니다 — 바로 코드를 뽑게 하지 않고, AI가 먼저 만들 순서를 말하게 한 뒤 진행하면 결과가 훨씬 깔끔합니다

**실습**

1. 새 폴더를 하나 만들고, 아래 계획 먼저 프롬프트를 넣어 미술관 사이트의 뼈대를 생성합니다
2. AI가 내놓은 계획을 한 번 읽고 "좋아, 그대로 만들어줘"로 진행합니다 — 계획이 마음에 안 들면 이 단계에서 고쳐 달라고 하면 됩니다
3. 완성된 페이지를 브라우저로 엽니다. 랜덤 사진 30장이 격자로 뜨면 1차 성공 — 새로고침 할 때마다 사진이 바뀌는 것도 확인해 보세요
4. 아래 개선 프롬프트로 전시회다운 디테일을 얹어 완성도를 높입니다

**EXAMPLE PROMPT (계획 먼저 — 미술관 뼈대)**
```
https://picsum.photos/300 처럼 요청하면 300×300 랜덤 이미지를 돌려주는 무료 API가 있어. 키나 가입은 필요 없어.
이걸 이용해 가상 사진 전시회(온라인 미술관)처럼 보이는 웹사이트를 만들어줘.
이미지는 30장 정도를 격자(그리드)로 보기 좋게 배치하면 돼.
바로 코드부터 짜지 말고, 어떻게 만들지 계획부터 세워서 보여줘.
```

**EXAMPLE PROMPT (개선 ① — 작품처럼 꾸미기)**
```
좋아. 이제 각 사진을 액자에 넣은 작품처럼 보이게 해줘.
사진마다 아래에 작품 제목과 작가 이름을 그럴싸하게 붙이고(예: "무제 No.12 — 김바로"),
페이지 맨 위에 미술관 이름과 한 줄 소개도 넣어줘.
```

**EXAMPLE PROMPT (개선 ② — 움직임 넣기)**
```
마우스를 사진 위에 올리면 살짝 커지면서 제목이 도드라지게 해줘.
사진을 클릭하면 큰 화면으로 확대돼 감상할 수 있으면 더 좋아.
```

> 💡 [캡처: picsum으로 만든 미술관 사이트 화면 — 랜덤 사진 30장이 격자로 놓이고, 각 사진 아래에 제목·작가명이 붙은 전시회 모습]

#### 잘 안 될 때

| 증상 | 해결 |
| --- | --- |
| 사진 자리가 **빈 네모(깨진 이미지)**로만 보임 | 인터넷 연결을 확인하고 새로고침. 그래도 안 뜨면 Claude에게 **"이미지가 안 나와. 주소가 `https://picsum.photos/300` 형식이 맞는지 확인해줘"** |
| 사진 30장이 **전부 똑같은 한 장**임 | 같은 주소를 30번 부르면 브라우저가 같은 사진을 재활용할 수 있다. Claude에게 **"사진마다 다르게 나오게 해줘"**라고 하면 주소를 조금씩 다르게 만들어 준다 |
| 사진이 **세로로 한 줄**로만 쌓임 | 격자가 아직 안 잡힌 것. **"사진을 여러 열의 격자로 정렬해줘"**라고 요청 |

#### 이 장을 마치면

- [ ] 키도 가입도 없이, API로 받아온 사진 30장이 격자로 놓인 미술관 사이트가 브라우저에 떠 있다
- [ ] "주소로 요청하면 데이터가 오고, 그걸 화면에 뿌린다"는 API의 한 바퀴를 직접 겪었다
- [ ] 이 감각을 밑천으로, 다음 세션부터 키가 필요한 진짜 API로 넘어갈 준비가 됐다

---

### 구글 API 서비스 — 크롬 익스텐션으로 만들기

_DAY 2 · 11:00–12:00 · 프로젝트 3_

> 웹페이지가 아니라, **크롬에 실제로 설치해서 쓰는 프로그램**을 만듭니다.

> (도식: 크롬 익스텐션 구조 도식)
>
> 파일 3개짜리 폴더가 곧 익스텐션이다 — 셋 다 Claude가 만들어준다

#### 핵심 내용

- 크롬 익스텐션 = 크롬 브라우저에 설치하는 작은 프로그램. 광고 차단기, 번역기가 전부 익스텐션이다 — 오늘 그걸 직접 만든다
- 예시 주제: YouTube Data API로 "채널 이름을 넣으면 구독자 수와 최근 영상을 보여주는 익스텐션", Google Maps API로 "우리 동네 즐겨찾기 지도" 등
- 스토어 등록 없이도 내 크롬에는 즉시 설치할 수 있다 — 이걸 "개발자 모드 로드"라고 부른다

**실습 순서**

1. 새 폴더를 만들고, 아래 프롬프트로 익스텐션을 생성한다
2. 크롬 주소창에 chrome://extensions 입력 → 우상단 개발자 모드 스위치 켜기
3. 압축해제된 확장 프로그램을 로드합니다 클릭 → 내 폴더 선택 — 폴더를 열었을 때 manifest.json이 바로 보이는 그 폴더를 골라야 한다. 한 단계 위 폴더를 고르면 "manifest가 없다"는 오류가 난다
4. 크롬 우상단 퍼즐 조각 아이콘에서 내 익스텐션을 찾아 클릭 — 실행!
5. 구글 API에서 받아온 실제 데이터가 팝업에 뜨면 완성

**EXAMPLE PROMPT**
```
크롬 익스텐션을 만들어줘. 아이콘을 누르면 작은 팝업이 뜨고,
유튜브 채널 이름을 검색하면 YouTube Data API로
구독자 수와 최근 영상 목록을 보여주는 기능이야.
API 키는 발급받아 뒀어. 키를 넣을 위치만 알려주면 내가 직접 넣을게.
```

#### 잘 안 될 때

| 증상 | 해결 |
| --- | --- |
| 익스텐션 카드에 빨간 **"오류"** 버튼이 보임 | 버튼을 눌러 메시지를 복사한 뒤 Claude에게: **"익스텐션 로드가 안 돼. 이 에러 고쳐줘"** + 붙여넣기 |
| 코드를 고쳤는데 바뀐 게 없음 | `chrome://extensions`에서 내 익스텐션 카드의 **새로고침(↻)** 버튼을 눌러야 반영된다 — 다들 한 번씩 잊는 단계다 |
| 팝업이 하얗게만 뜸 | 팝업 위에서 우클릭 → **검사** → Console의 빨간 글씨를 캡처해 Claude에게: **"팝업이 빈 화면으로 떠. 이 에러 고쳐줘"** |

#### 이 장을 마치면

- [ ] 크롬 우상단 퍼즐 아이콘 안에 내가 만든 익스텐션이 있다
- [ ] 클릭하면 구글 API에서 받아온 진짜 데이터가 팝업에 뜬다

---

### 공공 API + 데이터 시각화 + AI 답변 서비스

_DAY 2 · 13:00–14:50 · 프로젝트 4_

> 오후에 각자 완주하는 프로젝트는 이것 하나입니다. 서두르지 말고 체크포인트를 하나씩 통과하세요.

> (도식: 공공 API 시각화 AI 서비스 구조 도식)

#### 핵심 내용

- 공공데이터포털(data.go.kr) = 정부가 공개한 데이터 창고. 날씨, 미세먼지, 버스 도착 정보 같은 데이터를 무료 API로 받을 수 있다
- "AI가 데이터 기반으로 답한다"는 건 별게 아니다 — 사용자의 질문과 함께 방금 받아온 데이터를 AI에게 건네주며 "이 데이터를 보고 답해줘"라고 시키는 것
- 페이지 안의 AI는 지금 쓰는 클로드 코드와는 별개로, AI API 키가 따로 필요하다 — 어떤 AI를 어떻게 연결할지는 수업에서 키와 함께 안내한다 (혼자 해본다면: "이 페이지에 AI 답변 기능을 붙이고 싶어. 방법을 제안해줘"라고 물어보는 것부터)
- 어제와 오늘 배운 것이 전부 조립되는 시간이다 — 프론트엔드(Day 1) + API(오늘 오전) + AI

**단계별 체크포인트 — 순서대로, 하나씩**

1. API 응답(JSON)이 F12 콘솔에 찍힌다 — 여기까지가 절반이다
2. 받아온 숫자로 차트가 화면에 그려진다
3. 질문을 입력하면 데이터를 근거로 답이 온다

**EXAMPLE PROMPT (시작)**
```
공공데이터포털의 미세먼지 API를 쓰는 웹페이지를 만들자.
단계별로 가자. 먼저 API를 호출해서 응답 데이터가
브라우저 콘솔에 찍히는 것까지만 해줘.
인증키는 발급받아 뒀어. 넣을 위치를 알려줘.
```

> 💡 막히면 어제의 디버깅 3원칙 — ① 에러 전문 붙여넣기 ② "기대 vs 실제"로 말하기 ③ 한 번에 하나만. API 프로젝트에서 특히 강력합니다.

#### 잘 안 될 때

| 증상 | 해결 |
| --- | --- |
| "등록되지 않은 키" 류의 에러 | 공공데이터포털 키는 **일반 키와 인코딩 키 2가지**다 — 서로 바꿔 넣으면 실패한다. Claude에게: **"인코딩 키로 바꿔서 다시 호출해줘"** |
| 콘솔에 **CORS**라는 빨간 에러 | 브라우저가 외부 요청을 막은 것 — 내 잘못이 아니다. Claude에게: **"CORS 오류가 나. 우회 방법을 적용해줘"** |
| 에러는 없는데 데이터가 안 옴 | F12 → Network 탭의 빨간 줄을 캡처해 Claude에게: **"요청은 가는데 데이터가 안 와. 원인 찾아줘"** |

#### 이 장을 마치면

- [ ] F12 콘솔에서 API 응답(JSON)을 내 눈으로 봤다
- [ ] 받아온 데이터가 차트로 그려진다
- [ ] 질문을 입력하면 데이터를 근거로 답이 돌아온다

---

### 쇼핑몰 + 결제 연결 — 화면 따라 함께 만들기

_DAY 2 · 15:00–16:40 · 따라하기 세션_

> 이번 시간은 각자 만들기가 아니라 **강사 화면을 따라 함께** 갑니다. 목표는 "결제 성공" 화면을 전원이 보는 것.

> (도식: 결제 흐름 도식)

#### 핵심 내용

- 테스트 결제란: 결제 회사(토스페이먼츠)가 연습용으로 제공하는 가짜 결제. 키가 test_로 시작하는 한 실제 돈은 1원도 빠져나가지 않는다
- 키는 두 종류다 — 클라이언트 키(test_ck_, 결제창을 띄우는 용도, 화면에 보여도 괜찮음) / 시크릿 키(test_sk_, 결제를 확정하는 용도, 절대 공개 금지). 어제 배운 "키는 비밀번호다"가 여기서 실전이 된다
- 내 키는 어디서 보나 — developers.tosspayments.com에 로그인하면 내 개발정보 메뉴에 테스트 키 두 개가 나란히 있다. 오늘은 준비된 공용 키로 진행하지만, 집에서 혼자 해볼 때를 위해 위치를 기억해두자
- 오늘의 흐름: 준비된 쇼핑몰 스타터 → 상품 목록 → 장바구니 → 결제 버튼 → 토스 결제창 → 결제 성공 화면
- 스타터 파일과 공용 테스트 키는 수업 시작할 때 배포된다 — 받은 폴더를 열고 강사 화면과 같은 위치에서 출발하면 된다
- 주문 내역은 저장되어야 하고(DB), "진짜 결제됐는지" 확인은 사용자 화면이 아니라 서버가 해야 안전하다(백엔드) — 화면의 금액 표시나 버튼 숨김은 보안이 아니다
- 따라오다 막히면 바로 손들기 — 결제 연동은 원래 한 글자만 틀려도 멈춘다. 부끄러운 게 아니다

**테스트 카드 — 이 번호로 결제해본다**

1. 카드번호: 4330-0000-0000-0001
2. 유효기간: 아무 미래 날짜 / CVC: 아무 3자리 / 비밀번호: 앞 2자리 아무 숫자
3. 결제 성공 화면이 뜨면 — 오늘의 목표 달성 🎉

#### 잘 안 될 때

| 증상 | 해결 |
| --- | --- |
| 결제 위젯이 안 뜸 | 클라이언트 키가 `test_ck_`로 시작하는지, 앞뒤 공백이 없는지 확인 |
| 위젯은 뜨는데 결제 실패 | 시크릿 키 확인 — 키를 서로 바꿔 넣은 경우가 많다 |
| 하얀 빈 화면 | F12 → Console의 빨간 글씨를 캡처해 Claude에게 |
| 키 발급이 안 됨 | 손 들기 — 수업용 공용 테스트 키로 진행 |

> 💡 쇼핑몰을 끝까지 제대로 만들어보고 싶다면 — 오늘 배운 API·결제 위에 기능을 하나씩 더 붙여 나가면 됩니다.

#### 이 장을 마치면

- [ ] 테스트 카드로 "결제 성공" 화면을 직접 봤다
- [ ] 결제 확인을 왜 화면이 아니라 서버가 해야 하는지 설명할 수 있다

#### 더 깊이 — 공식 자료

**참고 자료**

- [토스페이먼츠 개발자센터 (가입·테스트 키 발급)](https://developers.tosspayments.com) — developers.tosspayments.com
- [결제위젯 연동 가이드 (공식 문서)](https://docs.tosspayments.com/guides/v2/payment-widget/integration) — docs.tosspayments.com
- [결제 테스트하는 방법 (테스트 카드 안내)](https://docs.tosspayments.com/blog/how-to-test-toss-payments) — docs.tosspayments.com

---

### Day 2 회고

_DAY 2 · 16:40–17:00 · 회고_

> 오늘은 하루 만에 익스텐션, 데이터 서비스, 결제까지 지나왔습니다. 정리하고 내일을 준비합니다.

#### 함께 하기

- 서비스 시연 릴레이 — 내 익스텐션과 시각화 서비스를 자랑하자
- 테스트 결제까지 뚫린 사람 박수 👏

#### 스스로 점검

- API란 무엇인가? 요청과 응답의 흐름을 설명할 수 있나?
- API 키를 깃허브에 올리면 왜 위험한가?
- 결제에서 백엔드와 DB는 각각 왜 필요했나?

> 💡 **내일 예고** — 내일은 데이터베이스로 진짜 저장되는 앱(노션 클론)을 만들어 인터넷에 배포합니다. 특별한 준비물은 없어요.

## DAY 3 — 확장

---

### 확장 — 데이터를 저장하고, 인터넷에 공개한다

_DAY 3_

> 오늘의 목표: 데이터베이스를 이해하고, 진짜로 저장되는 노션 클론을 만들어 인터넷에 배포한다.

> 오늘의 산출물 — **데이터베이스에 저장되는 노션 클론 + Vercel로 공개된 진짜 인터넷 주소**

오전에는 데이터가 어디에 저장되는지(**데이터베이스**)를 배우고, 오후에는 **진짜로 저장되는 노션 클론**을 만들어 **Vercel로 인터넷에 공개**합니다. "내 브라우저에만" 있던 데이터가 "모두의 저장소"로 넘어가는 하루입니다.

#### 시간표

| 시간 | 구분 | 세션 |
| --- | --- | --- |
| 09:00–09:30 | 복습 | [전체 흐름 복습](#d3-1) |
| 09:30–10:50 | 이론+실습 | [데이터베이스 입문](#d3-db) |
| 11:00–12:00 | 따라하기 | [노션 클론 만들기](#d3-notion) |
| 13:00–14:30 | 따라하기 | [노션 클론 (이어서)](#d3-notion) |
| 14:40–15:50 | 이론+실습 | [Vercel 배포](#d3-vercel) |
| 16:00–17:00 | 마무리 | [이후 학습 로드맵](#d3-8) |

---

### 전체 흐름 복습

_DAY 3 · 09:00–09:20 · 복습_

> 큰 그림이 오늘 완성됩니다: 만들기 → **저장하기** → 인터넷에 공개하기. 오늘은 데이터가 진짜로 **저장되는 앱**을 만들어 배포까지 마칩니다.

#### 지금까지 온 길

- Day 1 — 프롬프트로 만들고(구현), 고치고(디버깅), GitHub Pages로 공개했다(배포)
- Day 2 — API로 바깥 데이터를 연결했고, 크롬 익스텐션이라는 새 형태를 경험했고, 결제에서 백엔드·DB의 필요성을 실감했다
- Day 3(오늘) — 데이터베이스를 배우고, 진짜로 저장되는 노션 클론을 만들어 Vercel로 인터넷에 공개한다

#### 함께 하기

- 오늘 만들 노션 클론이 어떤 앱인지 가볍게 그려보기 — 페이지를 만들고, 저장하고, 어디서든 다시 여는 앱이다

---

### 데이터베이스 입문 — 내 데이터를 어디에 저장하나

_DAY 3 · 09:30–10:50 · 이론+실습_

> 지금까지 만든 앱들은 데이터가 **내 브라우저 안에만** 갇혀 있었습니다. 여러 사람이 함께 쓰는 진짜 서비스로 넘어가려면 **데이터베이스**가 필요합니다. 다음 세션의 노션 클론을 위한 발판입니다.

#### 먼저, 오늘 만들 앱을 상상해 봅시다

- 잠시 뒤 우리가 직접 만들 localStorage 할 일 앱을 떠올려 봅시다. 새로고침해도 내용이 남습니다 — 브라우저가 내 컴퓨터 한구석에 적어두기 때문입니다
- 그런데 여기엔 결정적인 한계가 있습니다 — 그 데이터는 "내 브라우저에만" 저장됩니다. 다른 기기(내 폰), 다른 사람(친구)에게는 절대 보이지 않습니다. 심지어 같은 컴퓨터라도 다른 브라우저를 켜면 텅 비어 있습니다
- 즉 localStorage는 혼자 쓰는 개인 메모장일 뿐입니다. "친구와 같이 쓰는 할 일 목록", "어느 기기서든 이어지는 노트" 같은 걸 만들려면 데이터를 모두가 접근하는 한곳에 모아야 합니다. 그곳이 바로 데이터베이스입니다

#### 데이터베이스란 무엇인가

데이터베이스(DB)는 두 가지가 합쳐진 것입니다 — 데이터를 쌓아두는 **공간**과, 그 데이터를 안전하게 넣고 꺼내주는 **관리 시스템**.

> 💡 비유하자면 **거대한 도서관**입니다. 책을 꽂아두는 **서가(공간)**만 있으면 금방 아수라장이 됩니다. 어디에 무엇을 두고, 누가 빌려갔고, 어떻게 빨리 찾아줄지 관리하는 **사서(관리 시스템)**가 있어야 도서관이 됩니다. DB도 마찬가지로 **저장 공간 + 똑똑한 사서**가 한 세트입니다.

- 사서가 있으니 수백만 건이 쌓여도 원하는 데이터를 순식간에 찾고, 여러 사람이 동시에 요청해도 뒤엉키지 않게 정리해 줍니다
- 우리는 이 사서에게 "이런 걸 넣어줘 / 저런 걸 꺼내줘"라고 부탁만 하면 됩니다. 창고를 직접 뒤질 필요가 없습니다

#### 두 가지 종류 맛보기 — 관계형 vs 문서형

데이터베이스에도 성격이 다른 종류가 있습니다. 깊은 내용은 심화에서 다루고, 지금은 **이런 게 있다** 정도만 알면 충분합니다.

|  | 관계형 (SQL) | 문서형 (NoSQL) |
| --- | --- | --- |
| **생김새** | 엑셀 표처럼 **행·열**이 딱 정해진 칸 | 자유로운 **서류 뭉치** — 칸이 유연함 |
| **비유** | 📊 줄이 딱 맞는 **회계 장부** | 🗂️ 내용이 제각각인 **서류철** |
| **강점** | 데이터끼리 **관계로 연결**(회원↔주문)해 정확하게 관리 | 형식이 바뀌어도 유연하게 담기, 빠른 개발 |
| **대표 예** | PostgreSQL, MySQL, **Supabase** | MongoDB, Firebase |

> 💡 [캡처: 관계형(줄 맞춘 엑셀 표) vs 문서형(내용이 제각각인 서류 카드 뭉치)을 나란히 놓은 개념 그림]

- 왜 알아둬야 할까요? — 무엇을 만드느냐에 따라 적절한 것을 골라 AI에게 지시해야 하기 때문입니다. "회원과 주문이 얽힌 쇼핑몰"은 관계형이 어울리고, "형식이 자주 바뀌는 메모 뭉치"는 문서형이 편할 수 있습니다
- 다음 세션에서 쓸 Supabase는 관계형(엑셀 표 방식)입니다. 우리에게 익숙한 표 형태라 이해하기 쉽습니다

#### 데이터로 하는 일은 딱 4가지 — CRUD

메모 앱이든 노션이든 쇼핑몰이든, 데이터로 하는 일은 결국 이 **네 가지의 조합**입니다. 앞 글자를 따 **CRUD(크러드)**라고 부릅니다.

| 동작 | 뜻 | 메모 앱에서라면 |
| --- | --- | --- |
| **C**reate (만들기) | 새 데이터를 넣는다 | 새 메모를 쓴다 |
| **R**ead (읽기) | 저장된 데이터를 꺼내 본다 | 메모 목록을 펼쳐 본다 |
| **U**pdate (수정) | 기존 데이터를 고친다 | 메모 내용을 고친다 |
| **D**elete (삭제) | 데이터를 지운다 | 메모를 버린다 |

- 이 네 단어만 알면 어떤 앱이든 "무엇을 만들고, 읽고, 고치고, 지우는지"로 쪼개어 AI에게 시킬 수 있습니다. 노션 클론도 결국 페이지에 대한 CRUD일 뿐입니다

**실습 — 저장을 눈으로 확인하고, 그 한계까지 만나기**

1. 새 폴더에서 Claude에게 아래 계획 먼저(plan-first) 프롬프트를 줍니다. 계획이 나오면 한 번 읽어보고 실행합니다
2. 완성되면 할 일을 3~4개 추가해 봅니다 (Create). 목록이 화면에 뜹니다 (Read)
3. 이제 핵심 — 브라우저를 새로고침(F5) 합니다. 놀랍게도 방금 쓴 할 일이 그대로 남아 있습니다. localStorage가 데이터를 저장한 것입니다. 데이터가 "저장된다"는 걸 눈으로 확인하는 순간입니다
4. 한계 확인 — 같은 페이지 주소를 다른 브라우저(예: 크롬에서 만들었다면 엣지)나 휴대폰에서 열어봅니다. 텅 비어 있습니다. "이 데이터는 내 브라우저에만 있었구나"를 몸으로 확인합니다. 이게 바로 다음 세션에서 진짜 DB가 필요한 이유입니다

**EXAMPLE PROMPT (계획 먼저 — plan-first)**
```
바로 만들지 말고 계획부터 보여줘. 브라우저의 localStorage를 써서 새로고침해도 목록이 남는 간단한 할 일 앱을 HTML 파일 하나로 만들 거야. 기능은 딱 네 가지 — 할 일 추가, 목록 보기, 완료 체크(수정), 삭제. 계획이 마음에 들면 그때 만들어줘.
```

> 💡 지금은 **localStorage**라 데이터가 내 브라우저 안에만 남습니다. "왜 폰에서는 안 보이지?"라는 이 답답함이, 다음 세션에서 진짜 데이터베이스로 넘어가는 **출발점**입니다.

#### 그래서 다음 세션에서는

- 오늘 배운 개념(데이터베이스 · 관계형 · CRUD)을 그대로 써서, 이번엔 진짜 데이터베이스인 Supabase에 저장되는 노션 클론을 만듭니다
- 그러면 localStorage의 한계가 사라집니다 — 어느 기기에서든, 여러 사람이 함께 같은 데이터를 보고 고칠 수 있게 됩니다. "내 브라우저에만"에서 "모두의 저장소로" 넘어가는 순간입니다

#### 이 장을 마치면

- [ ] 데이터베이스가 "저장 공간 + 관리 시스템(사서)"이라는 걸 도서관에 비유해 설명할 수 있다
- [ ] 관계형과 문서형의 차이를 한 문장으로 말할 수 있고, Supabase가 관계형이라는 걸 안다
- [ ] CRUD 네 가지가 무엇인지 말할 수 있다
- [ ] localStorage로 저장을 눈으로 확인하고, "내 브라우저에만" 남는 한계까지 직접 만나봤다

---

### 노션 클론 만들기 — 내 데이터를 저장하는 앱 (따라하기)

_DAY 3 · 11:00–12:00 · 13:00–14:30 · 따라하기_

> 이번 시간은 각자 만들기가 아니라 **강사 화면을 따라 함께** 갑니다. 앞 세션 「데이터베이스 입문」에서 말로만 배운 걸, 오늘 **진짜로 저장되는 앱**에 붙여봅니다. 목표는 전원이 "페이지를 만들고 → 새로고침해도 남아 있는" 화면을 보는 것.

> (도식: 노션 클론 CRUD 흐름 도식)

#### 노션이 뭔가요

- 노션(Notion)은 문서·메모·할 일·표를 한 곳에서 자유롭게 만드는 올인원 노트 앱입니다. 페이지를 만들고, 그 안에 또 페이지를 넣고, 언제든 고치고 지울 수 있는 게 특징이에요
- 많은 사람이 "이런 거 나도 만들어보고 싶다"고 생각하는 대표적인 앱이라, 오늘의 따라하기 소재로 딱 좋습니다
- 진짜 노션은 기능이 아주 많지만, 우리는 그 뼈대 하나만 그대로 따라 만듭니다 — 페이지를 만들고 저장하는 부분이요

#### 오늘 만드는 것

- 노션처럼 페이지를 만들고(Create)·목록으로 보고(Read)·수정하고(Update)·삭제(Delete) 되는 간단한 앱. 이 네 가지를 앞글자만 따서 CRUD(크러드)라고 부릅니다 — 데이터 앱의 기본 4동작이에요
- 이 네 가지가 되면, 사실상 "내 데이터를 저장하는 앱"의 뼈대는 다 갖춘 셈입니다. 메모장·가계부·일기·독서기록 — 전부 같은 CRUD 위에 얹혀 있어요
- 회원가입·소셜 로그인(구글로 로그인 같은 것)은 오늘은 하지 않습니다. 그건 심화로 미루고, 오늘은 CRUD 하나에만 집중합니다. 욕심내면 하루로 안 끝나요

> 💡 앞 세션에서 "데이터는 껐다 켜도 남아야 한다 → 그래서 데이터베이스가 필요하다"고 배웠죠. 오늘 그 말이 눈앞에서 증명됩니다 — **페이지를 만들고 브라우저를 완전히 껐다 다시 열어도 그대로 남아 있는** 순간이 오늘의 하이라이트입니다.

#### 오늘은 "따라하기"입니다 — 공용 환경을 받아 씁니다

- 지금까지는 파일 하나(index.html)를 더블클릭해 열었지만, DB가 붙는 앱은 '서버가 도는 프로젝트'라 폴더를 VS Code로 열고 터미널 명령으로 실행합니다. 그래서 오늘은 강사가 준 스타터 폴더로 시작합니다
- 데이터를 저장하려면 Supabase(수파베이스)라는 데이터베이스 서비스가 필요합니다. 그런데 각자 가입부터 → 프로젝트 만들고 → 표(테이블) 설계하고 → 키 발급받고… 이걸 다 하면 하루로도 부족합니다
- 그래서 오늘은 어제 결제 따라하기와 똑같은 방식으로 갑니다 — 강사가 미리 준비한 Supabase 프로젝트(공용 키 + 스타터 폴더)를 나눠드리고, 여러분은 그걸 받아서 붙이기만 합니다
- 스타터 폴더와 공용 키는 수업 시작할 때 배포됩니다. 받은 폴더를 열고 강사 화면과 같은 위치에서 출발하면 돼요
- 키는 비밀번호다 — 어제 배운 원칙 그대로입니다. 오늘 받는 키는 연습용 공용 키라 함께 써도 되지만, 화면 캡처를 SNS에 올릴 땐 키가 찍히지 않게 조심하는 습관을 들이세요

> 💡 **집에서 혼자 할 때는요?** 각자 supabase.com에 무료로 가입해 프로젝트를 만들고, 거기서 나오는 **내 키 2개**를 붙이면 오늘과 똑같이 됩니다. 처음부터 끝까지 혼자 세팅하는 방법은 **심화 과정**에서 단계별로 따로 안내합니다 — 오늘은 원리를 몸에 익히는 게 먼저예요.

#### 오늘의 순서 — 4단계

| 단계 | 무엇을 | 왜 |
| --- | --- | --- |
| **① 스타터 받기** | 강사가 준 폴더와 공용 키를 받아 연다 | 모두 같은 출발선에 서기 위해 |
| **② 화면(UI) 먼저** | 노션처럼 보이는 겉모습을 먼저 만든다 | 기능보다 "보이는 것"이 있어야 붙일 자리가 생긴다 |
| **③ DB 연결(CRUD)** | Supabase를 연결해 저장·불러오기를 붙인다 | 오늘의 핵심 — 데이터가 진짜로 남게 |
| **④ 동작 확인** | 내 컴퓨터(localhost)에서 만들고·지워본다 | 배포 전에 잘 되는지 눈으로 확인 |

> 💡 중요한 순서 하나 — **화면(②)을 먼저, 저장(③)은 그다음**입니다. 겉모습과 데이터 연결을 한 번에 시키면 AI도 헷갈리고 어디서 틀렸는지 찾기 어려워요. "화면부터 → 그다음 저장"으로 **작업을 쪼개는** 것 자체가 오늘의 기술입니다.

**따라하기 — 강사 화면과 함께**

1. 스타터 열기 — 배포된 스타터 폴더를 VS Code로 열고, 터미널에서 강사가 안내하는 실행 명령을 그대로 입력한다. 브라우저에 빈 시작 화면이 뜨면 준비 완료
2. 공용 키 붙이기 — 강사가 준 Supabase 주소·키를 안내된 설정 파일의 정해진 칸에 붙여넣는다 (앞뒤 공백이 끼지 않게 주의). 이 값이 "우리 데이터가 저장될 곳" 주소다
3. ② UI 먼저 만들기 — 아래 첫 번째 프롬프트로 노션처럼 보이는 화면만 먼저 만든다. 이 단계에선 아직 저장이 안 돼도 정상이다 (새로고침하면 사라짐)
4. ③ CRUD 붙이기 — 화면이 마음에 들면, 두 번째 프롬프트로 Supabase를 연결해 만들기·보기·수정·삭제를 붙인다
5. ④ 확인 — 페이지를 몇 개 만들어 보고, 하나는 수정, 하나는 삭제해 본다. 그리고 브라우저를 껐다 다시 열어 — 방금 만든 페이지가 그대로 있으면 오늘의 목표 달성 🎉
6. 따라오다 막히면 바로 손들기. 키 한 글자, 공백 하나만 틀려도 저장이 멈춥니다. 부끄러운 게 아니에요
7. [각자 5분] 강사를 따라 완성했으면, 이제 혼자 페이지 제목 색이나 폰트 하나만 바꿔 보자 — 내 손으로 고쳐보는 게 진짜 실력이 된다

**EXAMPLE PROMPT (② UI 먼저 — 룩앤필만)**
```
노션(https://www.notion.com/ko)과 비슷한 느낌의 "페이지 관리 앱" 화면을 만들어줘.
지금은 기능은 말고 룩앤필만 — 왼쪽에 페이지 목록, 오른쪽에 내용 영역이 있는 노션처럼 보이는 깔끔한 화면이면 좋겠어.
저장 기능은 다음 단계에서 붙일 거니까 지금은 화면만. 바로 만들지 말고 계획부터 세워서 보여줘.
```

**EXAMPLE PROMPT (③ DB 연결 — CRUD 붙이기)**
```
이제 이 앱에 데이터베이스를 연결해서, 페이지를 만들고·목록으로 보고·수정·삭제(CRUD)가 되게 해줘.
데이터베이스는 내가 제공한 Supabase 설정(주소·키)을 그대로 사용해줘.
나중에 Vercel 배포까지 할 예정이니 그때 문제가 안 생기는 최신 연결 방법을 인터넷으로 확인해서 적용해줘.
한꺼번에 하지 말고, 계획부터 세워서 보여주고 단계적으로 진행하자.
```

[캡처: 완성된 노션 클론 화면 — 왼쪽 페이지 목록, 오른쪽 내용 편집 영역]

[캡처: Supabase 대시보드의 테이블에 내가 만든 페이지들이 행으로 쌓인 화면]

#### 자주 막히는 곳

| 증상 | 해결 |
| --- | --- |
| 만든 페이지가 **저장이 안 됨** | Supabase **주소·키**가 설정 파일에 제대로 들어갔는지, 앞뒤 **공백**이 없는지 확인. 두 값을 서로 바꿔 넣은 경우도 많다 |
| **새로고침하면 사라짐** | 아직 DB에 연결되지 않고 **화면(브라우저)에만 임시로** 담긴 상태다. ③ 단계(CRUD 붙이기)를 마쳤는지 확인 — 진짜 저장은 Supabase에 들어가야 남는다 |
| 화면이 **하얀 빈 화면** | `F12` → Console의 **빨간 글씨**를 그대로 캡처해 Claude에게 보여주기. 에러 전문이 최고의 단서다 |
| 저장은 되는데 **목록에 안 보임** | Supabase 대시보드에서 데이터는 들어갔는지 먼저 확인. 데이터는 있는데 화면에만 안 나오면 "목록 불러오기(Read)" 부분 문제 — 그대로 Claude에게 전달 |

> 💡 "새로고침하면 사라진다"와 "껐다 켜도 남는다"의 차이가 바로 **앞 세션에서 배운 데이터베이스의 존재 이유**입니다. 사라지면 실패가 아니라 — 아직 DB에 안 붙었다는 **신호**일 뿐이에요.

#### 다음은 — 진짜 인터넷에 공개

- 지금 이 앱은 내 컴퓨터(localhost)에서만 돌아갑니다. 브라우저 주소가 localhost로 시작하죠 — 이건 나만 볼 수 있는 주소예요
- 그래서 다음 세션에서 Vercel(버셀)로 배포해, 이 노션 클론을 진짜 인터넷 주소로 공개합니다. 링크만 보내면 친구도 자기 폰에서 열어볼 수 있게요
- ③ 프롬프트에서 "배포까지 고려해 최신 방법으로" 부탁해 둔 이유가 여기 있습니다 — 지금 잘 만들어 두면 다음 배포가 매끄럽습니다

#### 이 장을 마치면

- [ ] 노션처럼 페이지를 만들고·보고·수정·삭제하는 앱을 직접 움직여봤다
- [ ] 브라우저를 껐다 켜도 데이터가 남는 것을 눈으로 확인했다 — 데이터베이스가 왜 필요한지 실감했다
- [ ] 강사 제공 공용 Supabase 환경을 내 앱에 붙여봤고, 혼자 할 땐 무엇이 더 필요한지 안다
- [ ] 이 앱이 아직 내 컴퓨터에서만 돌아간다는 것과, 다음 세션에서 Vercel로 공개한다는 흐름을 이해했다

---

### Vercel 배포 — DB 붙은 앱을 인터넷에 공개하기

_DAY 3 · 14:40–15:50 · 이론+실습_

> 앞 세션에서 만든 **노션 클론**은 아직 **내 컴퓨터 안에서만** 열립니다. 이번 장에서는 그 앱을 진짜 인터넷 주소에 올려, 링크만 보내면 누구나 쓰게 만듭니다.

#### 지금 상태 — localhost는 나만 볼 수 있다

- 노션 클론을 실행하면 주소창에 localhost:3000 같은 주소가 떴을 겁니다. 여기서 localhost = 바로 이 컴퓨터라는 뜻입니다 — 내 컴퓨터에서만 열리는 주소이고, 옆 사람에게 이 주소를 보내도 열리지 않습니다
- Day 1에서 프로필 페이지를 GitHub Pages로 공개했던 것처럼, 이번엔 이 앱을 인터넷에 올릴 차례입니다. 그런데 이번 앱은 DB가 붙어 있어서 방법이 조금 다릅니다 — 왜 다른지부터 봅니다

#### 정적 배포 vs 동적 배포 — 왜 GitHub Pages로는 안 되나

> Day 1의 프로필 페이지와 오늘의 노션 클론은 **종류가 다른 웹**입니다. 이 차이를 알면 "왜 새 서비스를 배워야 하나"가 한 번에 이해됩니다.

|  | 정적(static) 배포 | 동적(dynamic) 배포 |
| --- | --- | --- |
| **어떤 앱** | Day 1 프로필 페이지 — HTML 파일만 있는 페이지 | 오늘의 노션 클론 — **DB와 연결되고 서버가 도는 앱** |
| **필요한 것** | 파일만 있으면 됨 (서버·DB 불필요) | 요청이 올 때마다 **서버가 DB를 읽고 써야** 함 |
| **배포 수단** | **GitHub Pages** (무료로 그냥 올림) | GitHub Pages로는 **못 올린다** → **Vercel** 같은 서비스 |
| **비유** | 벽에 붙여둔 **인쇄된 포스터** — 누가 봐도 똑같음 | 손님마다 응대하는 **사람이 있는 가게** — 그때그때 반응 |

- GitHub Pages는 완성된 파일을 그대로 보여주기만 합니다. 뒤에서 일해줄 서버가 없어서, "글을 저장하면 DB에 넣어라" 같은 움직이는 일을 시킬 데가 없습니다
- 그래서 동적 앱은 서버를 대신 굴려주는 배포 서비스가 필요합니다. 오늘 쓰는 것이 Vercel입니다

#### Vercel — GitHub만 연결하면 알아서 올려주는 서비스

- Vercel(버셀, vercel.com) = 내 GitHub 저장소를 연결하면 자동으로 빌드·배포해주는 서비스. 동적 앱에 필요한 서버까지 알아서 준비해 줍니다
- 무료로 시작할 수 있고, 개인 프로젝트나 오늘 만든 노션 클론 정도는 무료 범위로 충분합니다
- 한 번 연결해 두면, 이후에는 GitHub에 새로 올리기(푸시)만 하면 Vercel이 알아서 다시 배포합니다 — Day 1에서 파일을 다시 올리면 사이트가 갱신되던 것과 같은 느낌입니다

> 💡 Day 1에서 배운 **커밋(포장)·푸시(발송)**가 여기서 그대로 쓰입니다. Vercel은 그 "발송된 택배(GitHub 저장소)"를 받아 자동으로 진열대에 올려주는 **배달 대행**이라고 생각하면 됩니다.

#### 배포 4단계 — 큰 그림

| 단계 | 하는 일 | 한 줄 요령 |
| --- | --- | --- |
| **① GitHub에 올리기** | 노션 클론 폴더를 저장소로 올린다 | Day 1에서 배운 커밋·푸시 그대로 — "이 프로젝트 GitHub에 올려줘" 한 마디면 됨 |
| **② Vercel 연결** | Vercel에 GitHub 계정으로 로그인하고 저장소를 고른다 | 목록에서 방금 올린 저장소를 클릭 → Import |
| **③ 환경변수 등록** | Supabase 키 등을 Vercel에 안전하게 넣는다 | 키를 **코드가 아니라 Vercel에** 붙여둔다 (아래 상세) |
| **④ 배포·공유** | Deploy 누르고 나온 주소를 연다 | `...vercel.app` 주소가 곧 내 서비스 링크 |

#### ③ 환경변수 — 키는 코드에 넣지 않는다

> Day 1에서 **"API 키는 환경변수로 보관한다"**고 배웠습니다. Vercel 배포에서 그 개념을 실제로 씁니다. 노션 클론은 DB(Supabase)에 접속하려면 **비밀 키**가 필요한데, 이 키를 코드 안에 적어 두면 안 됩니다.

- 환경변수 = 프로그램이 실행될 때 읽어가는 바깥에 붙여둔 설정 메모. 키를 코드에 직접 쓰지 않고, 이 메모에 적어두면 코드는 "그 메모를 읽어" 사용합니다
- 왜 이렇게 하나 — 키가 코드에 박혀 있으면, 코드를 GitHub에 올리는 순간 키까지 온 세상에 공개됩니다. 남이 내 DB를 마음대로 열 수 있게 되는 셈입니다
- 그래서 키는 Vercel의 환경변수 칸(Environment Variables)에 따로 등록합니다 — 코드에는 "키를 여기서 읽어와" 하는 표시만 남고, 진짜 값은 Vercel이 안전하게 보관합니다

| 흔히 넣는 환경변수 | 무엇 |
| --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | 내 Supabase 프로젝트 주소 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase 접속용 공개 키 |

> 💡 정확히 어떤 이름의 환경변수가 필요한지는 **프로젝트마다 다릅니다**. 외우지 말고 **Claude에게 물어보세요** — 아래 프롬프트처럼 부탁하면 내 코드를 읽고 필요한 변수 목록을 그대로 짚어 줍니다.

#### 막히면 Claude에게 — 절차 통째로 물어보기

비개발자는 화면(GUI)만으로 충분히 배포할 수 있습니다. 다만 어디를 눌러야 할지 헷갈릴 때, 아래처럼 부탁하면 내 프로젝트에 맞춰 안내해 줍니다.

**EXAMPLE PROMPT (환경변수 확인)**
```
이 프로젝트를 Vercel에 배포하려고 해.
내 코드를 읽고, 배포하려면 어떤 환경변수가 필요한지 이름과 함께 알려줘.
그리고 그 값들을 각각 어디서 구하는지도 설명해줘.
```

**EXAMPLE PROMPT (배포 절차 안내)**
```
이 노션 클론을 Vercel에 처음 배포하려고 해.
GitHub에 올리는 것부터 Vercel 연결, 환경변수 등록, 배포까지
비개발자가 따라 할 수 있게 화면 클릭 위주로 단계별로 안내해줘.
```

> 💡 개발자들은 **Vercel CLI**(터미널에서 `vercel` 명령으로 배포하는 도구)를 쓰기도 합니다. 알아두면 좋지만, **오늘은 웹 화면 클릭만으로** 끝까지 갑니다 — 익숙해진 뒤에 Claude에게 "CLI로 배포하는 법도 알려줘"라고 물어보면 됩니다.

**따라하기 — 노션 클론을 Vercel에 올리기**

1. 먼저 프로젝트를 GitHub에 올린다 — Claude에게 "이 프로젝트를 GitHub에 새 저장소로 올려줘" 한 마디면 저장소 생성부터 커밋·푸시까지 처리해 준다 (Day 1에서 배운 그대로)
2. vercel.com 접속 → Continue with GitHub로 로그인 (GitHub 계정을 그대로 사용, 별도 가입 불필요)
3. Add New… → Project → 방금 올린 저장소를 목록에서 찾아 Import 클릭 [캡처: Vercel 프로젝트 연결 화면]
4. Environment Variables 칸을 펼쳐, 위에서 확인한 이름과 값(Supabase URL·키 등)을 하나씩 넣는다 — 이름 칸에 변수 이름, 값 칸에 실제 키를 붙여넣고 Add [캡처: Vercel 환경변수 설정 화면]
5. Deploy를 누르고 잠시 기다린다 — Vercel이 자동으로 빌드하고 서버를 올린다
6. 완료되면 내프로젝트.vercel.app 같은 주소가 나온다. 눌러서 열어보고, 내 폰으로도 열어본다 — DB에 글을 저장했다가 새로고침해도 남아 있으면 성공 [캡처: 배포 완료된 주소]
7. 이제 이 주소를 옆 사람에게 보내 서로의 노션 클론에 글을 남겨본다

> 💡 **혼자 해보기** — 배포된 내 노션 클론에 **나만의 기능 하나**(예: 페이지에 날짜 표시, 검색창)를 Claude에게 시켜 붙이고, 다시 커밋·푸시하면 Vercel이 자동으로 새로 배포한다. 스스로 붙인 첫 기능이다.

#### 보안 — 키가 GitHub에 올라가면 벌어지는 일

- 키·시크릿은 절대 코드에 직접 적지 말고 환경변수로 — 이 한 가지만 지켜도 대부분의 사고를 막습니다
- 실수로 키가 GitHub에 올라가면, Supabase·구글 같은 서비스가 이를 감지해 그 키를 자동으로 무효화하기도 합니다. 앱이 갑자기 멈추는 원인이 되니, "키를 코드에 넣지 않는다"는 습관이 중요합니다
- 만약 이미 올라갔다면 당황하지 말고 Claude에게 "이 키를 코드에서 빼고 환경변수로 바꿔줘. 그리고 새 키로 교체하는 방법도 알려줘"라고 부탁하면 됩니다

#### 정리 — 이제 정적·동적 둘 다 배포할 수 있다

|  | Day 1 — 정적 | Day 3 — 동적 |
| --- | --- | --- |
| **배포 수단** | GitHub Pages | Vercel |
| **어떤 결과물** | 프로필 페이지 (파일만) | 노션 클론 (DB 붙은 앱) |
| **주소 모양** | `아이디.github.io/…` | `…vercel.app` |

- 이제 어떤 걸 만들든 인터넷에 공개하는 길이 둘 생겼습니다 — 파일뿐인 페이지는 GitHub Pages로, DB·서버가 필요한 앱은 Vercel로. 만들 것의 성격에 맞춰 고르면 됩니다
- 기획 → 구현 → 디버깅 → 배포로 이어지는 3일 전체 흐름이, 정적·동적 양쪽 모두에서 완성됐습니다

#### 이 장을 마치면

- [ ] 정적 배포(GitHub Pages)와 동적 배포(Vercel)가 왜 다른지 한 문장으로 설명할 수 있다
- [ ] 노션 클론이 …vercel.app 주소로 열리고, 폰으로도 접속된다
- [ ] Supabase 키를 코드가 아니라 Vercel 환경변수에 등록했다
- [ ] 키를 코드에 넣으면 왜 위험한지 알고, 옆 사람과 링크를 주고받아 봤다

#### 더 깊이 — 공식 자료

**참고 자료**

- [Vercel 시작하기 (공식 문서)](https://vercel.com/docs/getting-started-with-vercel) — vercel.com
- [환경변수 설정하기 (공식 문서)](https://vercel.com/docs/projects/environment-variables) — vercel.com

---

### 이후 학습 로드맵

_DAY 3 · 16:40–17:00 · 마무리_

> 3일이 끝나도 혼자서 계속 만들 수 있는 길을 챙겨갑니다.

#### 혼자 계속하는 법

| 상황 | 이렇게 한다 |
| --- | --- |
| **만들 것이 떠오르면** | 기획 3문항(누가 / 기능 하나 / 형태)부터 — 이번 주에 하나 더 만들어보자 |
| **배포하고 싶으면** | GitHub Pages는 계속 무료다. 프로젝트마다 저장소 하나면 된다 |
| **데이터가 필요하면** | 공공데이터포털(data.go.kr), Google Cloud — 아이디어는 대부분 "이 데이터 + 이 화면"의 조합이다 |
| **막히면** | 디버깅 3원칙 + 부록 「막혔을 때 응급처치」. 에러는 무서운 게 아니라 힌트다 |
| **분석을 붙이고 싶으면** | 부록 「MS Clarity」를 따라 내 사이트에 설치 — 방문자가 어디를 누르는지 데이터가 쌓인다 |

## 부록

---

### 수강 전 준비 체크리스트

_부록_

> 아래 항목은 **수업 전날까지** 완료해주세요. 특히 공공데이터포털은 신청 승인에 시간이 걸리니 꼭 미리!

- [ ] GitHub 계정 가입 (github.com) — Day 1 오후, 내 웹페이지를 인터넷에 공개할 때 사용
- [ ] Google 계정 준비 + Google Cloud 콘솔(console.cloud.google.com) 로그인 확인 — Day 2 구글 API 키 발급용
- [ ] 공공데이터포털 가입 (data.go.kr) + 미세먼지·날씨 등 관심 API 활용신청 미리 제출 — 승인까지 시간이 걸릴 수 있음
- [ ] Claude 유료 구독(Pro 이상) 준비 — 아쉽게도 무료 계정으로는 클로드 코드를 쓸 수 없다. 요금제는 claude.com/pricing에서 확인
- [ ] VS Code 설치 (code.visualstudio.com)
- [ ] Node.js LTS 설치 (nodejs.org) — 클로드 코드와 웹 프로젝트 실행의 기본 엔진. 설치 방법은 부록 「설치 상세 가이드」 참고
- [ ] Git 설치 (git-scm.com) — 코드 버전 관리·배포에 사용. 상세 절차는 부록 「설치 상세 가이드」 참고
- [ ] Python 설치 (python.org) — 설치 중 "Add Python to PATH" 체크 필수. 상세 절차는 부록 「설치 상세 가이드」 참고
- [ ] Claude Code(클로드 코드) 설치 — 이 수업의 주력 도구. 설치·환경변수 설정은 부록 「설치 상세 가이드」에서 단계별로 안내
- [ ] 토스페이먼츠 테스트 키는 수업에서 제공되므로 따로 준비할 것 없음

> 💡 준비하다 막히는 항목이 있어도 괜찮습니다 — 수업 첫날 아침에 함께 해결할 시간이 있습니다. 단, **공공데이터포털 활용신청만은 꼭 미리** 넣어두세요.

---

### 설치 상세 가이드 — 도구 4종 한 번에

_부록_

> 준비 체크리스트에서 이름만 봤던 도구들을 **실제로 설치하는 화면 단위 절차**입니다. 순서대로 하나씩 따라오면 됩니다. 막히면 그 도구 항목의 **버전 확인 명령**부터 다시 해보세요 — 결과가 나오면 설치 성공입니다.

#### 시작 전에 — "터미널"이 뭔가요?

- 터미널은 컴퓨터에 글자로 명령을 내리는 검은 창입니다. 아래 설치들이 잘 됐는지 확인할 때 씁니다
- 윈도우: 시작 버튼 → terminal 또는 PowerShell 검색 → 열기
- 맥: command+space → terminal 입력 → 열기
- 이 창에 아래 버전 확인 명령을 그대로 치고 Enter를 누르면 됩니다

> 💡 버전 확인 명령을 쳤을 때 숫자(예: `v22.11.0`)가 나오면 설치 성공, **"명령을 찾을 수 없습니다 / not recognized"**가 나오면 설치가 안 됐거나 **터미널을 껐다 다시 켜야** 합니다. 설치 후에는 항상 터미널을 새로 여세요.

#### ① Node.js — 웹과 클로드 코드의 기본 엔진

> 클로드 코드를 설치하려면 Node.js가 먼저 있어야 합니다. 웹 프로젝트를 돌리는 데도 쓰입니다.

1. 다운로드: https://nodejs.org 접속 → 큰 버튼 두 개 중 "LTS"라고 적힌 쪽을 클릭 (LTS = 안정 버전, 초보자 권장). [캡처: nodejs.org 다운로드 화면 — LTS 버튼 강조]
2. 설치 실행: 내려받은 설치 파일을 더블클릭 → Next(다음)만 계속 눌러 진행 → 마지막에 Install/Finish
3. 설치 중 "Tools for Native Modules" 같은 추가 체크박스가 나와도 그냥 기본값으로 두고 Next 하면 됩니다
4. 버전 확인: 터미널을 새로 열고 아래 두 명령을 차례로 입력 [캡처: 터미널에서 node -v 와 npm -v 실행 결과]
  | 명령 | 확인 내용 | 정상 예시 |
  | --- | --- | --- |
  | `node -v` | Node.js 버전 | `v22.11.0` |
  | `npm -v` | 함께 깔리는 npm 버전 | `10.9.0` |

> 💡 `npm`은 Node.js를 설치하면 **자동으로 함께** 깔립니다. 따로 설치할 필요 없습니다.

#### ② Git — 코드 저장·배포 도구

> Day 1 오후에 내 웹페이지를 인터넷에 공개(배포)할 때 씁니다. 미리 깔아두세요.

1. 다운로드: https://git-scm.com/downloads 접속 → 본인 운영체제(Windows / macOS) 클릭 → 다운로드 시작. [캡처: git-scm 다운로드 페이지]
2. 설치 실행 (윈도우): 설치 파일 더블클릭 → 옵션 화면이 여러 번 나오지만 전부 기본값 그대로 Next → Install
3. 설치 실행 (맥): 보통 이미 깔려 있는 경우가 많습니다. 아래 버전 확인이 되면 추가 설치가 필요 없습니다
4. 버전 확인: 터미널을 새로 열고 git --version 입력 → git version 2.47.0 처럼 나오면 성공. [캡처: git --version 실행 결과]

> 💡 윈도우 설치 중 "기본 에디터 선택" 화면이 나와 당황할 수 있는데, **그냥 기본값으로 두고 Next** 하면 수업에 지장 없습니다.

#### ③ Python — 설치 중 체크박스 하나가 핵심

> 일부 프로젝트·도구가 Python을 사용합니다. 설치 자체보다 **체크박스 하나**가 더 중요합니다.

1. 다운로드: https://www.python.org/downloads/ 접속 → 노란 "Download Python 3.x" 버튼 클릭. [캡처: python.org 다운로드 화면]
2. 설치 실행 — 가장 중요: 설치 파일을 더블클릭하면 첫 화면 아래쪽에 "Add Python to PATH" 체크박스가 있습니다. 반드시 먼저 체크하세요. 이걸 놓치면 터미널에서 python 명령을 못 찾습니다. [캡처: "Add Python to PATH" 체크박스에 체크한 화면]
3. 체크를 확인했으면 "Install Now" 클릭 → 완료까지 대기 → Close
4. 버전 확인: 터미널을 새로 열고 python --version 입력 → Python 3.13.1 처럼 나오면 성공
  - 맥에서 안 되면 python3 --version 으로 시도해 보세요

> 💡 **"Add Python to PATH"를 깜빡했다면?** 당황하지 말고 설치 파일을 다시 실행 → **"Modify(수정)"** 선택 → PATH 옵션을 켜고 다시 진행하면 됩니다. 처음부터 다시 깔 필요 없습니다.

#### ④ Claude Code(클로드 코드) — 이 수업의 주력 도구

> 위 ①~③이 끝나야 설치할 수 있습니다. 특히 **Node.js가 먼저** 깔려 있어야 합니다. 유료 Claude 구독(Pro 이상)도 필요합니다.

**(a) 설치 전 준비**

- Node.js 설치 확인: 터미널에서 node -v가 버전을 출력하는지 다시 확인
- Claude 계정: 유료 구독 상태로 claude.com에 로그인되는지 확인 (무료 계정으로는 사용 불가 — 준비 체크리스트 참고)

**(b) 설치** — 이 수업은 Day 1 「개발환경 구성」 세션에서 클로드 코드를 **PowerShell 네이티브 설치** 방식으로 깝니다(설치 경로 `%USERPROFILE%\.local\bin`). 여기서는 설치가 끝난 뒤의 **확인**과 **환경변수 정리**만 다룹니다.

> 💡 [확인 필요: 클로드 코드 공식 설치 명령/다운로드 — 최신 형태를 공식 문서 `docs.claude.com`의 Claude Code 설치 페이지에서 확정하세요. 네이티브 설치(PowerShell)와 npm 설치(`@anthropic-ai/claude-code`) 두 방식이 있을 수 있습니다.]

- 설치가 끝나면 버전 확인: claude --version 입력 → 버전 번호가 나오면 성공
- 실행: 프로젝트 폴더에서 claude 라고만 치면 클로드 코드가 시작됩니다

**(c) 윈도우 환경변수(PATH) 추가** — `claude` 명령을 "찾을 수 없다"고 나올 때만 하면 됩니다

설치는 됐는데 터미널이 `claude` 명령을 못 찾는 경우, 컴퓨터에게 "그 프로그램이 어디 있는지" 알려주는 작업이 PATH 추가입니다.

1. 시작 버튼 → 환경 변수 검색 → "시스템 환경 변수 편집" 클릭 (또는 제어판 → 시스템 및 보안 → 시스템 → 고급 시스템 설정)
2. 열린 "시스템 속성" 창 아래쪽 "환경 변수(N)..." 버튼 클릭
3. 아래 "사용자 변수"에서 "Path"를 찾아 클릭 → "편집(E)..." 버튼 클릭. [캡처: 환경 변수 편집 창 — Path 선택 상태]
4. "새로 만들기(N)" 클릭 → 클로드 코드가 설치된 폴더 경로 %USERPROFILE%\.local\bin 를 붙여넣기
5. "확인"을 모든 창에서 차례로 눌러 닫기 → 터미널을 완전히 껐다가 새로 열고 claude --version 재시도

> 💡 환경변수는 초보자가 가장 어려워하는 단계입니다. **혼자 씨름하지 말고** 이 화면을 캡처해서 강사에게 보여주거나, 클로드에게 "`claude` 명령을 못 찾는다는데 윈도우 PATH를 어떻게 확인하면 되냐"고 물어보세요. 대부분 5분이면 해결됩니다.

#### 이 장을 마치면

- [ ] node -v · npm -v가 버전을 출력한다
- [ ] git --version이 버전을 출력한다
- [ ] python --version이 버전을 출력한다 (설치 때 "Add Python to PATH" 체크함)
- [ ] claude --version이 버전을 출력하고, claude로 클로드 코드가 실행된다

---

### AI가 만든 것 검증하기

_부록_

> 이론 03에서 못 박았듯이, 도구를 붙여도 AI는 틀릴 수 있습니다. 코드를 못 읽어도 **확인할 수는 있습니다.**

#### 핵심 원칙 — 코드를 읽는 대신, 동작을 확인한다

- 인테리어 업체가 "다 끝났습니다!" 하면 그냥 믿고 입주하지 않죠. 수도꼭지를 틀어보고, 문을 열어보고, 콘센트를 꽂아봅니다. 도면을 읽을 줄 몰라도 검수는 할 수 있습니다
- AI가 만든 것도 똑같습니다. 코드를 한 줄도 못 읽어도 동작을 확인하는 방법은 다섯 가지나 있습니다

#### 방법 1 — 설명시키기

검증의 첫 수단은 코드가 아니라 **말**입니다. 설명을 듣고 "내가 시킨 거랑 다른데?" 싶으면 바로 수정을 요청하세요.

**EXAMPLE PROMPT**
```
방금 만든 코드를 비개발자한테 설명하듯이 알려줘.
- 어떤 파일을 만들었거나 수정했는지
- 각각 무슨 역할인지
- 사용자 입장에서 뭐가 달라지는지
```

#### 방법 2 — 이상한 입력을 넣어본다

- 정상적으로 쓰면 잘 됩니다. 일부러 틀리게 써보세요 — 가격 칸에 "만원"이라고 글자 입력, 필수 항목을 비우고 저장, 아주 긴 글 붙여넣기
- 문제를 찾으면 Day 1의 기대 vs 실제 형식으로 신고합니다

**EXAMPLE PROMPT (문제 신고)**
```
현재 상황: 가격에 "만원"이라고 글자를 입력했어.
결과: 에러 없이 등록되는데 가격이 "NaN원"으로 표시돼.
원하는 결과: 숫자만 입력 가능해야 하고, 글자를 넣으면 경고가 떠야 해.
```

#### 방법 3 — 다른 AI에게 크로스체크

내가 판단할 필요 없이 **AI들끼리 검증하게** 만드는 방법입니다. 추가 도구도 필요 없습니다.

| 순서 | 할 일 |
| --- | --- |
| ① | Claude에게 "결제 관련 코드 보여줘"라고 해서 코드를 받는다 |
| ② | 다른 AI(ChatGPT·Gemini 등)에 붙여넣고 "이 코드에 보안 문제나 버그가 없는지 확인해줘" |
| ③ | 지적받은 내용을 다시 Claude에게: **"다른 AI가 이런 문제를 지적했어: [내용] 이게 맞는지 확인하고, 문제가 있으면 고쳐줘"** |

#### 방법 4 — 보안 점검을 프롬프트 하나로

Day 2에서 배운 "키는 비밀번호다"를 **이론이 아니라 행동**으로 바꾸는 단계입니다.

**EXAMPLE PROMPT**
```
이 프로젝트 보안 점검해줘:
1. API 키나 비밀번호가 코드에 직접 적혀 있진 않은지
2. 브라우저에서 볼 수 있는 코드에 시크릿 키가 섞여 있진 않은지
3. 깃허브에 올리면 안 되는 파일이 포함돼 있진 않은지
결과를 정리해서 알려줘.
```

#### 방법 5 — 체크리스트를 통째로 AI에게 넘기기

- 체크리스트는 사람이 읽는 것이라는 고정관념을 버리세요. 확인할 항목을 쭉 적은 뒤 마지막에 한 줄 붙이면 됩니다 — "이 체크리스트대로 하나씩 확인해줘. 안 되는 게 있으면 고쳐줘."
- 교안에 있는 「이 장을 마치면」 체크리스트들도 그대로 복사해서 넘기면 됩니다

#### 직접 써보기 vs AI에게 시키기

|  | 내가 직접 써보기 | AI에게 점검시키기 |
| --- | --- | --- |
| **강점** | "뭔가 이상한데" 하는 직감, 쓰기 편한지 판단 | 빠르고, 시킨 건 빠짐없이 확인 |
| **약점** | 느리고 빠뜨린다 | 사용성·느낌은 판단 못 한다 |

> 💡 답은 **둘 다**입니다 — AI에게 기본 동작을 점검시키고, 내가 직접 써보며 느낌을 확인하는 것. 어느 하나만으로는 부족합니다.

#### 이 장을 마치면

- [ ] "코드를 못 읽는데 어떻게 확인하죠?"에 스스로 답할 수 있다
- [ ] 내 프로젝트에 이상한 입력을 넣어 최소 한 개의 문제를 찾아봤다
- [ ] 보안 점검 프롬프트를 한 번 돌려봤다

---

### 용어사전 — 쉬운 말 풀이

_부록_

> 수업 중 낯선 단어가 나오면 여기로 오세요. 목차 검색창에 단어를 쳐도 이 페이지가 잡힙니다.

#### AI · 프롬프트

|  |  |
| --- | --- |
| **바이브코딩** | 코드를 직접 쓰지 않고, 뭘 만들고 싶은지 말로 설명하면 AI가 만들어주는 개발 방식 |
| **프롬프트** | AI에게 보내는 지시문. 구체적일수록 결과가 좋아진다 |
| **토큰** | AI가 글을 읽고 쓰는 최소 단위. AI는 "다음 토큰"을 확률로 예측하며 답을 만든다 |
| **컨텍스트 윈도우** | AI가 한 번에 기억하는 대화의 양. 가득 찰수록 답이 흐려진다 — 작업 하나 끝나면 새 대화를 여는 이유 |
| **환각** | AI가 없는 사실을 그럴듯하게 지어내는 현상. 그래서 결과 검증이 필요하다 |
| **에이전트** | 목표를 주면 다음 단계를 스스로 정해 도구를 쓰며 여러 걸음을 가는 AI. 챗봇은 한 걸음(답변)에서 멈춘다 |
| **클로드 코드** | 터미널에서 도는 AI 코딩 에이전트. 파일을 직접 읽고, 고치고, 실행한다 |
| **CLAUDE.md** | 프로젝트 규칙·맥락을 적어두는 파일. 클로드 코드가 매 대화마다 자동으로 읽는다 |
| **Skill(스킬)** | 자주 하는 작업 절차를 등록해두고 `/이름` 한 줄로 재사용하는 기능. 엑셀 매크로와 비슷 |
| **훅(Hook)** | 정해진 시점에 반드시 실행되는 자동 명령. 규칙이 "부탁"이라면 훅은 "강제" |
| **서브에이전트** | 클로드 코드가 다른 AI 작업자를 불러 일을 나눠 시키는 기능. 동시에 일해서 빨라진다 |
| **MCP** | 클로드 코드에 외부 도구(브라우저·DB·문서 등)를 꽂는 표준 연결 규격 |

#### 웹 · 개발

|  |  |
| --- | --- |
| **프론트엔드** | 사용자 눈에 보이는 전부. HTML(뼈대)·CSS(모양)·JS(움직임)로 만든다 |
| **백엔드** | 화면 뒤에서 일하는 컴퓨터(서버). 로그인 확인·주문 접수처럼 화면에 못 맡기는 일을 한다 |
| **데이터베이스(DB)** | Database. 껐다 켜도 남아야 하는 데이터의 창고 — 회원 목록·주문 내역이 여기 산다 |
| **API** | Application Programming Interface. 다른 서비스의 데이터·기능을 가져다 쓰는 "주문 창구" — 정해진 형식으로 요청하면 응답이 온다 |
| **API 키** | "요청할 자격이 있어요"를 증명하는 열쇠. 비밀번호처럼 다뤄야 한다 |
| **JSON** | JavaScript Object Notation. 중괄호 `{ }`로 정리된 데이터 꾸러미 — API 응답의 표준 형식 |
| **변수** | 값에 붙인 이름표. `const price = 3000`처럼 '가격'을 부르는 방법 |
| **함수** | 이름 붙인 작업 묶음. 자주 하는 일을 한 단어로 시킨다 |
| **조건문** | "만약 ~라면"의 코드 표현(if) |
| **반복문** | "각각에 대해"의 코드 표현(for). 100건이어도 코드는 한 벌 |
| **환경변수** | 내 컴퓨터 전체가 기억하는 설정 메모. Path, API 키 보관에 쓴다 |
| **터미널** | 명령어로 컴퓨터를 조작하는 창(PowerShell). 클로드 코드가 여기서 돈다 |

#### 배포 · 운영

|  |  |
| --- | --- |
| **깃(Git)** | 코드의 세이브 포인트. 저장해두고 망치면 되돌린다 |
| **깃허브(GitHub)** | 코드를 올려 백업·공유하는 클라우드 저장소 |
| **커밋 / 푸시** | 커밋 = 포장(저장 지점 만들기), 푸시 = 발송(클라우드에 올리기) |
| **GitHub Pages** | 깃허브 저장소를 그대로 웹사이트로 켜주는 무료 기능 |
| **배포** | 내 컴퓨터에서만 열리던 것을 인터넷 주소로 누구나 볼 수 있게 공개하는 것 |
| **정적 페이지** | 서버·DB 없이 HTML 파일만으로 보여주는 페이지. GitHub Pages로 배포 가능 |
| **크롬 익스텐션** | 크롬 브라우저에 설치하는 작은 프로그램. 광고 차단기·번역기가 전부 이것 |
| **PWA** | Progressive Web App. 홈 화면에 설치되고 전체화면으로 열리는 웹앱 — 앱스토어 심사 없이 배포 |
| **토스페이먼츠** | 카드·간편결제를 서비스에 붙여주는 한국 PG(Payment Gateway, 결제대행) 서비스 |
| **테스트 결제** | 결제 회사가 제공하는 연습용 가짜 결제. 실제 돈은 1원도 안 나간다 |
| **MS Clarity** | 방문자가 내 사이트를 어떻게 쓰는지 보여주는 무료 분석 도구 |
| **히트맵** | 많이 클릭된 곳일수록 빨갛게 표시되는 지도. Clarity의 대표 기능 |

#### 개발 환경·도구

|  |  |
| --- | --- |
| **IDE** | Integrated Development Environment(통합개발환경). 코드편집기·터미널·확장프로그램·미리보기·파일탐색기를 **한 창에 모아둔** 개발용 프로그램. AI가 붙은 IDE로는 **VS Code, 커서(Cursor), 안티그래비티** 등이 있다 |
| **CLI** | Command Line Interface(명령줄 인터페이스). 그림 버튼 대신 **명령어를 글자로 입력해** 컴퓨터를 다루는 방식. 클로드 코드가 이 방식으로 동작한다 |
| **마크다운** | 특수 프로그램 없이 **텍스트만으로** 문서를 쓰고, `#`·`-` 같은 기호로 제목·목록 등 **의미를 부여**하는 문서 양식. CLAUDE.md·README가 모두 이것 |

#### TUI와 GUI

| 구분 | GUI | TUI |
| --- | --- | --- |
| **풀이** | Graphical User Interface<br>(그래픽 사용자 인터페이스) | Text-based User Interface<br>(텍스트 기반 사용자 인터페이스) |
| **조작법** | 마우스로 아이콘·버튼을 클릭 | 키보드로 글자(명령어)를 입력 |
| **예** | 윈도우 탐색기, 크롬, 한글 | 터미널, 클로드 코드 화면 |
| **강점** | 직관적이라 배우기 쉽다 | 빠르고 **자동화·기록**에 강하다 |
| **체감** | 이미 익숙한 방식 | 처음엔 낯설지만 바이브코딩은 대부분 여기서 이뤄진다 |

#### 라이브러리·프레임워크·모듈·컴포넌트·API

| 용어 | 한마디 | 비유 |
| --- | --- | --- |
| **라이브러리** | 필요한 기능을 골라 꺼내 쓰는 코드 묶음. 흐름의 **주도권은 나**에게 있다 | **도서관** — 필요한 책(기능)만 꺼내 내 방식대로 쓴다 |
| **프레임워크** | 정해진 뼈대·규칙 **안에** 내 코드를 끼워넣는 것. 자유도는 낮지만 **안정성↑** | **프랜차이즈** — 본사가 정한 구조 안에서 운영한다 |
| **모듈** | 기능을 잘게 나눠 담은 **파일 한 조각**. 필요한 것만 가져다 조립 | 레고 **블록 한 개** |
| **컴포넌트** | 화면을 이루는 **재사용 부품**(버튼·카드·입력창 등) | **조립 부품** — 버튼 하나를 여러 화면에서 다시 쓴다 |
| **API** | 다른 프로그램의 기능·데이터를 정해진 형식으로 불러 쓰는 창구 (웹·개발 항목 참고) | 식당 **주문 창구** — 주문(요청)하면 음식(응답)이 나온다 |

> 💡 왜 알아야 하나 — 에이전트에게 무언가 만들라고 시키면 **90%는 밑바닥부터 새로 짜는 게 아니라, 이미 검증된 라이브러리·프레임워크를 가져다 조립**합니다. 그래서 "○○ 라이브러리를 써서", "△△ 프레임워크 구조로 만들어줘"처럼 **이름과 개념을 알고 지시할수록** 원하는 결과가 정확히 나옵니다. 몰라도 만들어는 주지만, **무엇을 고르고 무엇으로 바꿀지 지시하려면** 이 단어들이 필요합니다.

---

### 막혔을 때 응급처치

_부록_

> 시간을 잡아먹는 건 개발이 아니라 가입·설치·연결입니다. **막히는 것은 여러분 잘못이 아닙니다** — 아래 순서대로 해보세요.

#### 무조건 먼저 해볼 3가지

- ① 에러 메시지(또는 화면)를 그대로 복사/캡처해서 Claude에게 "이 에러 해결해줘"
- ② 새 대화를 열고 다시 시켜보기 — 대화가 길어지면 판단이 흐려진다
- ③ 프로그램(터미널·VS Code)을 완전히 껐다가 다시 켜기 — "바꿨는데 반영이 안 됨"의 절반은 재시작으로 풀린다

> 💡 막힘의 3대 원인은 순서대로 — ① **계정**(가입·로그인·키가 잘못됨) ② **연결**(서비스끼리 이어지지 않음) ③ **재시작 안 함**(바꾼 게 반영 안 됨). 이 순서로 점검하면 대부분 잡힙니다.

#### 설치 · 명령어

| 증상 | 해결 |
| --- | --- |
| `claude` 명령을 찾을 수 없음 | 터미널을 닫았다 새로 열기 → 안 되면 PC 재시작 → 그래도 안 되면 Path에 `%USERPROFILE%\.local\bin` 등록 (Day 1 개발환경 페이지 참고) |
| 설치 명령이 안 먹힘 | 명령어를 한 글자도 빼지 말고 그대로 복사해 붙여넣었는지 확인 |
| 로그인 브라우저가 안 열림 | 터미널에 표시된 URL을 직접 복사해 브라우저 주소창에 붙여넣기 |
| GitHub 가입이 안 넘어감 | 스마트폰으로 가입한 뒤 PC에서 로그인. 인증 코드는 복사-붙여넣기로 |

#### GitHub Pages 배포

| 증상 | 해결 |
| --- | --- |
| 404 페이지가 뜸 | 첫 화면 파일명이 정확히 `index.html`인지 확인 (`Index.html`, `home.html` 안 됨 — 대소문자까지) |
| 고쳤는데 사이트가 그대로 | 첫 배포는 최대 10분 걸린다. 몇 분 기다린 뒤 **Ctrl+F5**(강력 새로고침) |
| Pages 메뉴를 켤 수 없음 | 저장소가 Private이기 때문 — Settings → General에서 **Public**으로 변경 |
| 화면은 뜨는데 모양이 깨짐 | Claude에게 "CSS 파일 경로 확인해줘" (파일명 대소문자 불일치가 흔한 원인) |

#### API · 결제

| 증상 | 해결 |
| --- | --- |
| API 응답이 안 옴 / 키 에러 | 키 앞뒤에 **공백**이 붙어 들어갔는지 확인 (복사할 때 가장 흔한 실수) → 키가 승인됐는지 발급 사이트에서 확인 |
| 결제 위젯이 안 뜸 | 클라이언트 키가 `test_ck_`로 시작하는지, 앞뒤 공백은 없는지 확인 |
| 키 발급 자체가 안 됨 | 손 들기 — 수업용 공용 테스트 키로 진행 |

#### 공통

| 증상 | 해결 |
| --- | --- |
| 하얀 빈 화면만 나옴 | F12 → Console 탭의 빨간 글씨를 캡처해서 Claude에게 전달 |
| 같은 걸 3번 고쳤는데 계속 실패 | 방법을 바꾼다 — "다른 방식으로 해결해줘" 또는 그 부분만 처음부터 다시 |
| 뭐가 문제인지 모르겠음 | "지금 프로젝트 상태를 분석해줘"라고 시키고 답을 같이 읽는다 |
| 아까는 됐는데 지금 안 됨 | ③번 응급처치 — 완전 종료 후 재시작부터 |

> 💡 그래도 안 되면 **손 들기** — 여러분이 막힌 곳은 다음 사람도 막히는 곳입니다. 질문이 수업을 좋게 만듭니다.

---

### 하네스 엔지니어링 — AI를 더 잘 부리는 기술

_부록_

> "하네스"는 말을 부릴 때 쓰는 장비입니다. AI를 더 안전하고 정확하게 부리는 세팅과 습관을 배웁니다.

#### 핵심 개념 — "부탁"과 "강제"

AI는 확률로 움직인다. 그래서 **부탁할 것(CLAUDE.md)과 강제할 것(훅)을 나누는 것**이 하네스 엔지니어링의 핵심이다.

|  | CLAUDE.md 규칙 | 훅(hook) |
| --- | --- | --- |
| **성격** | 부탁 | 강제 |
| **지켜지는 정도** | 거의 항상 (확률) | 무조건 100% (자동 실행) |
| **어울리는 일** | 말투, 스타일, 코딩 규칙 | 반드시 일어나야 하는 검사·차단 (예: 파일 고칠 때마다 자동 검사, 중요 파일 수정 차단) |

#### AI를 잘 부리는 4가지 기술

| 기술 | 무엇을 하나 | 예시 |
| --- | --- | --- |
| **권한 설정** | AI가 스스로 해도 되는 일의 범위를 정한다 | "파일 만들고 고치는 건 알아서, 지우는 건 물어보고" |
| **계획 모드** | 실행 전에 계획을 받아 검토한다 | 계획을 읽고 고친 뒤 실행 — 다시 만드는 일이 확 준다 |
| **작업 쪼개기** | 큰 요청 1개를 작은 요청 여러 개로 | "쇼핑몰" 1번보다 "상품 목록 → 장바구니 → 결제" 3번 |
| **서브에이전트** | AI 작업자들을 불러 동시에 나눠 시킨다 | 30분 걸릴 일이 10분에 (오늘은 개념만) |

#### 맡길까 말까 — 위임 판단 3질문

초보자가 가장 불안해하는 질문("이걸 AI한테 맡겨도 되나?")은 세 가지만 물어보면 대부분 풀립니다.

| 질문 | 예 → 맡겨도 좋다 | 아니오 → 사람이 한다 |
| --- | --- | --- |
| **되돌릴 수 있나?** | 코드 수정 (깃으로 복원 가능) | 메일 발송, 파일 삭제, 실제 결제 |
| **확인할 수 있나?** | 화면을 열어 눈으로 검증 가능 | 결과가 맞는지 확인할 방법이 없음 |
| **규칙이 분명한가?** | "이 형식으로 정리해줘" | 정답 없는 가치 판단, 책임이 큰 결정 |

> 💡 결제로 예를 들면 — **결제 연동 코드를 짜는 일은 맡기고, 실제 돈이 오가는 최종 확인은 사람이 합니다.** AI가 아무리 잘해도 최종 책임은 사람에게 있습니다.

#### 바이브코딩 핵심 습관 6가지

| 습관 | 왜 |
| --- | --- |
| ① 구체적으로 설명한다 | 비워둔 칸은 세상의 평균으로 채워진다 |
| ② 에러는 그대로 보여준다 | 에러 전문이 최고의 단서다 |
| ③ 작게 시작한다 | 큰 요청은 뒤로 갈수록 엉성해진다 |
| ④ 기능 하나마다 실행해 확인한다 | 문제를 일찍 잡을수록 고치기 쉽다 |
| ⑤ 첫 프롬프트에 "단계적으로 개발할 예정" 선언 | 완성 주문으로 오해하지 않게 한다 |
| ⑥ **작업 하나 끝나면 새 대화** | 대화가 길어지면 판단이 흐려진다 |

> 💡 이 기술들은 오늘 만들 **노션 클론**의 성패를 가릅니다 — 특히 **계획 모드로 시작**하고 **작업을 쪼개는** 습관은 제한된 수업 시간에서 가장 큰 무기가 됩니다.
> ⚠️ 남이 만든 훅 설정을 복사할 때는 반드시 내용을 확인하고 쓰세요 — 훅은 자동 실행되므로, 무슨 명령인지 모른 채 붙여넣으면 안 됩니다.

**실습 — 바로 쓸 프로젝트 세팅**

1. 새 프로젝트 폴더를 만들고, CLAUDE.md를 미리 작성해둔다 (어제 배운 것 활용)
2. 계획 모드로 "오늘 만들 것"의 계획을 한 번 받아보고, 마음에 안 드는 부분을 고쳐본다

#### 이 장을 마치면

- [ ] 프로젝트용 폴더와 CLAUDE.md가 준비되어 있다
- [ ] 계획 모드로 계획을 한 번 받아서, 마음에 안 드는 부분을 고쳐봤다

#### 더 깊이 — 공식 자료

**참고 자료**

- [훅(Hooks) 공식 가이드](https://code.claude.com/docs/en/hooks-guide) — code.claude.com
- [서브에이전트 공식 문서](https://code.claude.com/docs/en/sub-agents) — code.claude.com

---

### 앱처럼 쓰는 반응형 웹 — 내 폰에 설치되는 웹앱

_부록_

> Day 1에 만든 프로필 페이지가 오늘 **내 폰 홈 화면의 앱**이 됩니다.

> (도식: 설치되는 웹앱 흐름 도식)

#### 핵심 내용

- 앱처럼 쓰는 웹(설치되는 웹앱) = 홈 화면에 설치되고 전체화면으로 열리는 웹입니다. 아이콘으로 실행하면 겉보기엔 일반 앱과 거의 같습니다
- 필요한 것은 딱 두 가지 — manifest(앱 이름·아이콘 정보가 담긴 파일) + 서비스워커(설치를 가능하게 해주는 스크립트). 둘 다 Claude가 만들어준다
- 한 가지 조건이 있는데, 이렇게 설치되는 웹은 https 주소에서만 설치됩니다. 다행히 GitHub Pages 주소는 자동으로 https라서 — 어제 배포한 그대로면 조건이 이미 갖춰져 있습니다

#### 앱스토어 앱과 뭐가 다른가

|  | 설치되는 웹앱 (오늘) | 앱스토어 앱 |
| --- | --- | --- |
| **배포** | 심사·등록 없이 즉시 | 스토어 심사 필요 (며칠~몇 주) |
| **설치** | 브라우저에서 "홈 화면에 추가" | 스토어에서 다운로드 |
| **수정 반영** | 재배포하면 즉시 | 업데이트 심사 후 |
| **폰 기능** | 일부 제한 (알림 등) | 전부 사용 가능 |

- 오늘 우리의 핵심은 "설치되는 경험"이므로 이 방식이 딱 맞다

**실습 — 프로필 페이지를 앱으로**

1. Day 1 프로필 프로젝트 폴더를 열고, 아래 프롬프트를 보낸다
2. 바뀐 파일들을 깃허브에 다시 올린다 (Day 1 배포와 같은 방법)
3. 폰 브라우저로 내 주소를 열고 — 안드로이드(크롬): 메뉴(⋮) → 홈 화면에 추가 / 아이폰: 꼭 사파리로 열어서 공유 버튼 → 홈 화면에 추가 (아이폰용 크롬에는 이 메뉴가 없으니 헤매지 말자)
4. 홈 화면의 내 앱 아이콘을 눌러 전체화면으로 열리면 성공

**EXAMPLE PROMPT**
```
이 프로필 페이지를 홈 화면에 설치되는 웹앱으로 만들어줘.
홈 화면에 설치할 수 있게 manifest와 서비스워커를 추가하고,
앱 이름은 "OO의 프로필", 아이콘도 간단하게 만들어줘.
```

#### 이 장을 마치면

- [ ] 내 폰 홈 화면에 내가 만든 앱 아이콘이 있다
- [ ] 아이콘을 누르면 주소창 없이 전체화면으로 열린다

#### 더 깊이 — 공식 자료

**참고 자료**

- [홈 화면에 설치되는 웹앱 만들기 (MDN, 한국어)](https://developer.mozilla.org/ko/docs/Web/Progressive_web_apps) — developer.mozilla.org

---

### MS Clarity — 실사용자는 내 서비스를 어떻게 쓰는가

_부록_

> "만들었다"에서 끝나지 않고, 사람들이 실제로 어떻게 쓰는지 보고 고치는 것 — 그게 서비스 운영입니다.

> (도식: Clarity 사용자 추적 흐름 도식)

#### 핵심 내용

- MS Clarity = 마이크로소프트가 무료로 제공하는 사용자 행동 분석 도구. 내 사이트에 온 사람들이 어디를 누르고, 어디까지 읽고, 어디서 나가는지 보여준다
- 오늘 시연은 미리 설치해 데이터가 쌓여 있는 사이트로 본다 — Clarity는 설치 후 데이터가 대시보드에 뜨기까지 수십 분~수 시간이 걸려서, 여러분 사이트의 데이터는 내일 확인하게 된다

#### 무엇을 볼 수 있고, 보고 나서 무엇을 하나

| 기능 | 보여주는 것 | 보고 나서 할 일 |
| --- | --- | --- |
| **히트맵** | 많이 클릭된 곳일수록 빨갛게 | 아무도 안 누르는 버튼은 없앤다 |
| **세션 녹화** | 방문자가 쓰는 모습을 영상처럼 재생 | 다들 이탈하는 지점을 고친다 |
| **분노의 클릭** | 안 되는 버튼을 연타한 흔적 | 고장 났거나 헷갈리는 버튼을 찾아 고친다 |

**실습 — 내 사이트에 설치**

1. clarity.microsoft.com 접속 → 마이크로소프트 또는 구글 계정으로 가입
2. 새 프로젝트 만들기 → 프로젝트 이름과 내 배포 주소를 입력하고 만들기 → 설정 → 설치로 들어가면 "직접 설치" 탭에 추적 코드가 보인다 — 통째로 복사한다
3. Claude에게 "이 스크립트를 내 페이지의 head에 넣어줘"라고 하고 복사한 코드를 붙여넣는다
4. 깃허브에 다시 올려 재배포하면 설치 끝 — 데이터는 오늘 저녁~내일 대시보드에서 확인

#### 이 장을 마치면

- [ ] 추적 코드가 들어간 내 사이트가 다시 배포되어 있다
- [ ] 내일 대시보드에서 무엇을 볼지(히트맵·세션 녹화) 알고 있다

#### 더 깊이 — 공식 자료

**참고 자료**

- [MS Clarity (가입·대시보드)](https://clarity.microsoft.com) — clarity.microsoft.com
- [Clarity 공식 문서](https://learn.microsoft.com/en-us/clarity/) — learn.microsoft.com
